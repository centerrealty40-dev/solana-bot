# W8.0 Live Oscar — фазы внедрения (дорожная карта)

**Нормативная спека:** [`W8.0_live_oscar_trading_bot.md`](./W8.0_live_oscar_trading_bot.md)  
**Rollout §9 спеки:** `dry_run` (неделя) → `simulate` (микро-кошелёк) → `live` (микро-лимиты).

---

## Дерево фаз

```text
W8.0 Live Oscar
├── Phase 0 — Каркас и безопасный старт …………………………… ✅ (этот коммит)
│   ├── Решение: отдельный entrypoint `live-oscar` (не общий бинарь с pt1-*)
│   ├── `LIVE_*` конфиг (Zod) + guard путей журналов
│   ├── PM2 `live-oscar` (ENABLED=0, dry_run по умолчанию)
│   └── JSONL: `live_boot` + периодический `heartbeat` (без RPC / без подписи)
├── Phase 1 — Контракт live JSONL ………………………………………… ✅
│   ├── **Нормативная спека:** [`W8.0_phase1_live_jsonl_contract.md`](./W8.0_phase1_live_jsonl_contract.md) (`liveSchema: 1`, `intentId`, fsync-матрица)
│   ├── Реализация: `src/live/events.ts`, `intent.ts`, `store-jsonl.ts` (`appendLiveJsonlEvent`); тесты `tests/live-jsonl-phase1.test.ts`; продукт **1.4.0**
│   └── Следующий шаг: Phase 2 (Jupiter quote → unsigned tx)
├── Phase 2 — Jupiter quote → unsigned swap tx …………………… ☐
│   └── Переиспользование lite-api; `LIVE_JUPITER_*`
├── Phase 3 — Кошелёк + режим simulate ……………………………… ☐
│   ├── Keypair из файла (chmod 600); не читать ключ при ENABLED=0
│   └── simulateTransaction (политика qnCall vs прямой RPC — зафиксировать)
├── Phase 4 — Паритет Oscar: решения стратегии → adapter исполнения … ☐
├── Phase 5 — Риск §3.3 + капитал 2X §3.4 …………………………… ☐
├── Phase 6 — Режим live: send + подтверждение …………………… ☐
├── Phase 7 — Replay live JSONL + reconcile с цепью …………… ☐
└── Phase 8 — Наблюдаемость, алерты, чеклист RELEASE §10 для live-oscar … ☐
```

---

## Статус Phase 0 (реализовано)

| Элемент | Где |
|--------|-----|
| Entry | `src/scripts/live-oscar.ts` → `npm run live-oscar` |
| Конфиг | `src/live/config.ts` → `loadLiveOscarConfig()` |
| Журнал | `src/live/store-jsonl.ts` (**`appendLiveJsonlEvent`**), `src/live/events.ts` (Zod), `src/live/intent.ts` |
| Цикл | `src/live/main.ts` → heartbeat, SIGINT/SIGTERM |
| PM2 | `ecosystem.config.cjs` → приложение **`live-oscar`** (по умолчанию выкл.) |
| Тест guard путей | `tests/live-oscar-config.test.ts` |

**Запуск локально:** `LIVE_STRATEGY_ENABLED=0 LIVE_TRADES_PATH=./data/live/test-live.jsonl npm run live-oscar`  
**На VPS:** только `pm2 start ecosystem.config.cjs --only live-oscar` когда будете готовы; до включения торговли оставляйте `LIVE_STRATEGY_ENABLED=0`.

**Открытые решения §12 спеки** (до Phase 3–4): общий ли Jupiter Pro URL/key; обязательность reconcile при старте; учёт `simulate` в qn-feature meter.

---

## Чеклист перехода Phase 1 → Phase 2

- [ ] Jupiter lite-api quote + swap POST для live pubkey (`src/live/` или общий модуль).
- [ ] Заполнять `quoteSnapshot` на `execution_attempt` / результатах.

## Чеклист Phase 1 (выполнено)

- [x] Контракт [`W8.0_phase1_live_jsonl_contract.md`](./W8.0_phase1_live_jsonl_contract.md); **`appendLiveJsonlEvent`**.
- [x] Bump **1.4.0** + [`CHANGELOG.md`](../release/CHANGELOG.md).
