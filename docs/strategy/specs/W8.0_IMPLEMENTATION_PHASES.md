# W8.0 Live Oscar — фазы внедрения (дорожная карта)

**Нормативная спека:** [`W8.0_live_oscar_trading_bot.md`](./W8.0_live_oscar_trading_bot.md)  
**Rollout §9 спеки:** `dry_run` (неделя) → `simulate` (микро-кошелёк) → `live` (микро-лимиты).

---

## Дерево фаз

```text
W8.0 Live Oscar
├── Phase 0 — Каркас и безопасный старт …………………………… ✅ (этот коммит)
│   ├── Решение: отдельный entrypoint `live-oscar` (не общий бинарь с pt1-*)
│   ├── `LIVE_*` конфиг (Zod) + guard путей журналов
│   ├── PM2 `live-oscar` (ENABLED=0, dry_run по умолчанию)
│   └── JSONL: `live_boot` + периодический `heartbeat` (без RPC / без подписи)
├── Phase 1 — Контракт live JSONL ………………………………………… ✅
│   ├── **Нормативная спека:** [`W8.0_phase1_live_jsonl_contract.md`](./W8.0_phase1_live_jsonl_contract.md) (`liveSchema: 1`, `intentId`, fsync-матрица)
│   ├── Реализация: `src/live/events.ts`, `intent.ts`, `store-jsonl.ts` (`appendLiveJsonlEvent`); тесты `tests/live-jsonl-phase1.test.ts`; продукт **1.4.0**
│   └── Следующий шаг: Phase 4 (паритет стратегии → исполнение)
├── Phase 2 — Jupiter quote → unsigned swap tx …………………… ✅
│   ├── `src/live/jupiter.ts`, `jupiter-self-test.ts`; ENV `LIVE_JUPITER_*`, опционально `LIVE_PHASE2_*` self-test
│   └── Продукт **1.5.0**; тесты `tests/live-jupiter.test.ts`
├── Phase 3 — Кошелёк + режим simulate ……………………………… ✅
│   ├── **Нормативная спека:** [`W8.0_phase3_wallet_simulate_spec.md`](./W8.0_phase3_wallet_simulate_spec.md) (lazy keypair, подпись, `qnCall` feature `sim`, JSONL)
│   ├── Реализация: **`src/live/wallet.ts`**, **`simulate.ts`**, **`phase3-self-test.ts`**; ENV **`LIVE_SIM_*`**, **`LIVE_PHASE3_*`**; продукт **1.6.0**
│   ├── Keypair из файла (`chmod 600`); не читать ключ при ENABLED=0 или `dry_run` (**P3-I1**)
│   └── `simulateTransaction` только через **`qnCall`** + **`SA_RPC_HTTP_URL`** (как W7.8); без `sendTransaction`
├── Phase 4 — Паритет Oscar: решения стратегии → adapter исполнения … ✅
│   ├── **Нормативная спека:** [`W8.0_phase4_oscar_parity_adapter_spec.md`](./W8.0_phase4_oscar_parity_adapter_spec.md) — buy+sell+DCA в одном релизе; дашборд `/papertrader2`: фикс. порядок колонок Live→Oscar→Deep Runner→Dno
│   ├── Реализация: общий Oscar runtime в [`main.ts`](../../../src/papertrader/main.ts) (`journalAppend`, `liveOscar`, `skipPaperJsonlStore`); adapter [`phase4-execution.ts`](../../../src/live/phase4-execution.ts); Jupiter sell [`jupiter.ts`](../../../src/live/jupiter.ts); продукт **1.7.0**
│   └── Риск §3.3–§3.4 и `live` send — **не Phase 4** (Phase 5–6)
├── Phase 5 — Риск §3.3 + капитал 2X §3.4 …………………………… ✅
│   ├── **Нормативная спека:** [`W8.0_phase5_risk_capital_gates_spec.md`](./W8.0_phase5_risk_capital_gates_spec.md) — исполняемые гейты перед Phase 4 adapter; JSONL `risk_block` / `capital_skip` / `capital_rotate_close`; **`simulate`** и ротация капитала без on-chain finalize (до Phase 6); продукт **1.8.0**
│   └── On-chain **send** и §3.4 с реальным освобождением SOL — **Phase 6**
├── Phase 6 — Режим live: send + подтверждение …………………… ✅
│   └── **Нормативная спека:** [`W8.0_phase6_live_send_confirm_spec.md`](./W8.0_phase6_live_send_confirm_spec.md); реализация: [`phase6-send.ts`](../../../src/live/phase6-send.ts), расширение [`phase4-execution.ts`](../../../src/live/phase4-execution.ts); QN feature **`live_send`**; продукт **1.9.0**
├── Phase 7 — Replay live JSONL + reconcile с цепью …………… ☐
└── Phase 8 — Наблюдаемость, алерты, чеклист RELEASE §10 для live-oscar … ☐
```

---

## Статус Phase 0 (реализовано)

| Элемент | Где |
|--------|-----|
| Entry | `src/scripts/live-oscar.ts` → `npm run live-oscar` |
| Конфиг | `src/live/config.ts` → `loadLiveOscarConfig()` |
| Журнал | `store-jsonl.ts`, `events.ts`, `intent.ts`; Jupiter: **`jupiter.ts`**, **`jupiter-self-test.ts`** |
| Цикл | `src/live/main.ts` → heartbeat, SIGINT/SIGTERM |
| PM2 | `ecosystem.config.cjs` → приложение **`live-oscar`** (по умолчанию выкл.) |
| Тест guard путей | `tests/live-oscar-config.test.ts` |

**Запуск локально:** `LIVE_STRATEGY_ENABLED=0 LIVE_TRADES_PATH=./data/live/test-live.jsonl npm run live-oscar`  
**На VPS:** только `pm2 start ecosystem.config.cjs --only live-oscar` когда будете готовы; до включения торговли оставляйте `LIVE_STRATEGY_ENABLED=0`.

**Открытые решения §12 спеки** (до Phase 3–4): общий ли Jupiter Pro URL/key; обязательность reconcile при старте; учёт `simulate` в qn-feature meter.

---

## Чеклист перехода Phase 2 → Phase 3

См. DoD в [`W8.0_phase3_wallet_simulate_spec.md`](./W8.0_phase3_wallet_simulate_spec.md) §10.

- [x] Реализация по спеке **W8.0-p3**; bump **`VERSION`** + **`CHANGELOG`** (**1.6.0**).
- [x] Загрузка `LIVE_WALLET_SECRET` при **ENABLED=1** и **`simulate` \| `live`**; **`live`** send/confirm — Phase 6 (`phase6-send.ts`).
- [x] `simulateTransaction` только через **`qnCall`** (`feature: 'sim'`), учёт кредитов QN (`LIVE_SIM_CREDITS_PER_CALL`).

## Чеклист Phase 2 (выполнено)

- [x] Jupiter quote + swap POST, `quoteSnapshot` с результатом build — см. **`src/live/jupiter.ts`**.
- [x] Bump **1.5.0** + CHANGELOG.

## Чеклист Phase 1 (выполнено)

- [x] Контракт [`W8.0_phase1_live_jsonl_contract.md`](./W8.0_phase1_live_jsonl_contract.md); **`appendLiveJsonlEvent`**.
- [x] Bump **1.4.0** + [`CHANGELOG.md`](../release/CHANGELOG.md).
