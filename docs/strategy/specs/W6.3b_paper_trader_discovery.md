# Spec W6.3b — Paper-trader discovery (filters + dip + whale + BTC fix)

**Назначение spec:** для агента-исполнителя (cheap LLM). Выполнить последовательно, не отступая ни от одного шага.

**Контекст продукта:** `solana-alpha`. Репо: `c:/Users/cente/Ideas/solana-alpha`. Ветка `v2`. Текущий HEAD после W6.3a: `8a9de0eb267dee0dbc6d7148a4ec1786b60b9fac`.

**Время выполнения:** ~3–4 часа (это самый аналитически нагруженный из papertrader-этапов; SQL и фильтры пишутся 1-в-1 с salvage).

## Цель

После W6.3a у нас был skeleton с heartbeat'ами. После W6.3b paper-trader умеет:

1. Каждые `discoveryIntervalMs` (default 10s) делать **один UNION SQL** по 4 `*_pair_snapshots` для **POST/MIGRATION lanes**.
2. Применять фильтры: `evaluateSnapshot` (liq/vol/buys/sells/bs) + `globalGate` (token age / holders).
3. Для `STRATEGY_KIND=dip`: считать `evaluateDip` (drop% от high, impulse% high/low за DIP_LOOKBACK_MIN) + per-mint cooldown + (опционально) `fetchWhaleAnalysis` (creator dump / capitulation / DCA classifier).
4. Писать в JSONL **только** `eval` события c `{ pass, reasons, m, btc, whale_analysis }`. Никаких `open` / `close` — это W6.3c.
5. **Drive-by:** `pricing.refreshBtcContext` теперь использует **Binance public klines API** (`/api/v3/klines?symbol=BTCUSDT&interval=1h`) — даёт `ret1h_pct` и `ret4h_pct` сразу, на первой итерации. Прежний путь через `swaps` навсегда возвращал `null`, потому что `sa-stream` подписан только на pump.fun program, а wrapped BTC mints в `swaps` не появляются.

После W6.3b: новый PM2-app `pt1-dip-discovery-test` (заменяет старый `pt1-skeleton-test`) каждые 10 секунд эмитит ≥1 `eval` события за минуту, в каждом — `btc.ret1h_pct` число (не null).

## Жёсткие ограничения

- **НЕ реализовывать executor / `open` / `close` / `dca` / `tp_ladder` / `trailing` / `peak` / `partial_sell` / `followup_snapshot`** — это W6.3c. Если в JSONL появилась хоть одна строка с `kind != 'heartbeat' && kind != 'eval' && kind != 'eval-skip-open'` — спека выполнена неправильно.
- **НЕ создавать `discovery/launchpad.ts`-логику (Smart Lottery)** — только stub (см. ниже). То же для `discovery/fresh-validated.ts`.
- **НЕ модифицировать** `src/parser/**`, `src/stream/**`, `src/atlas/**`, `src/core/db/schema.ts`, миграции, `scripts-tmp/dashboard-server.ts`, `scripts-tmp/*-collector.mjs`, `scripts-tmp/jupiter-route-watcher.mjs`, `scripts-tmp/direct-lp-detector.mjs`.
- **НЕ модифицировать** `docs/platform/**`, `docs/strategy/W6_AUDIT.md` (только обновить `INDEX.md`).
- **НЕ модифицировать** уже существующие файлы из W6.3a кроме точечных правок: `pricing.ts` (BTC fix), `config.ts` (новые env-блоки), `main.ts` (заменить heartbeat-loop на discovery-loop), `types.ts` (добавить новые интерфейсы).
- **НЕ ломать DoD W6.3a.** Heartbeat-события продолжают писаться (но их теперь меньше — раз в `heartbeatIntervalMs`, как и было; discovery-tick идёт отдельным интервалом).
- **НЕ устанавливать новые npm-зависимости.** Всё уже есть.
- **НЕ пушить в `main`.** Только в `v2` через squash из `w6.3b-papertrader-discovery`.
- **НЕ использовать `git push --force`.**
- **НЕ создавать таблицы.** Storage = только JSONL.
- **НЕ запускать всё ecosystem.config.cjs** на VPS — только `pm2 restart pt1-skeleton-test --no-autorestart`, `pm2 delete pt1-skeleton-test`, потом `pm2 start ecosystem.config.cjs --only pt1-dip-discovery-test`. Чтобы не дёрнуть W6.1 коллекторы.

## Шаг 0 — Подготовка ветки

```bash
cd c:/Users/cente/Ideas/solana-alpha
git checkout v2
git pull origin v2
git rev-parse HEAD     # должно быть 8a9de0eb267dee0dbc6d7148a4ec1786b60b9fac
git checkout -b w6.3b-papertrader-discovery

# salvage worktree уже создан в W6.1; проверить:
ls ../_salvage_legacy/scripts-tmp/live-paper-trader.ts
```

## Шаг 1 — Drive-by фикс `pricing.ts` (BTC через Binance)

**Задача.** Заменить `refreshBtcContext` (читает `swaps` для wrapped BTC mints — не работает, потому что `sa-stream` только pump.fun) на источник Binance klines.

`PAPER_BTC_MINTS` config становится **не нужен** (Binance возвращает global BTC/USDT). Оставляем поле в `config.ts` для back-compat, но больше не используем.

### 1.1. Заменить тело `refreshBtcContext` в `src/papertrader/pricing.ts`

```typescript
// src/papertrader/pricing.ts (replace function body, keep signature)
export async function refreshBtcContext(_cfg: PaperTraderConfig): Promise<void> {
  // Binance public klines: 1h interval, last 5 candles (covers 4h+ lookback).
  // Free, no auth, returns historical immediately on first call.
  // Response shape: [openTime, open, high, low, close, volume, closeTime, ...][]
  const j = await fetchJson<unknown[][]>(
    'https://api.binance.com/api/v3/klines?symbol=BTCUSDT&interval=1h&limit=5',
  );
  if (!j || !Array.isArray(j) || j.length < 5) {
    btcRet1hPct = null;
    btcRet4hPct = null;
    return;
  }
  // Use the close of the latest fully-closed candle as "now" reference,
  // and prior closes for 1h-ago / 4h-ago.
  const closes = j.map((row) => Number(row[4])).filter((x) => Number.isFinite(x) && x > 0);
  if (closes.length < 5) {
    btcRet1hPct = null;
    btcRet4hPct = null;
    return;
  }
  const last = closes[closes.length - 1];
  const oneAgo = closes[closes.length - 2];
  const fourAgo = closes[closes.length - 5];
  btcRet1hPct = oneAgo > 0 ? (last / oneAgo - 1) * 100 : null;
  btcRet4hPct = fourAgo > 0 ? (last / fourAgo - 1) * 100 : null;
  btcLastUpdateTs = Date.now();
}
```

### 1.2. Удалить из `pricing.ts` импорты `dsql` и `db` если они больше не используются нигде в файле

После замены `refreshBtcContext` SQL остался только в `fetchLatestSnapshotPrice`. Импорты остаются нужными — **ничего не удаляй**, оставь как есть.

### 1.3. Smoke-проверка локально

```bash
node --input-type=module -e "
import('./dist/papertrader/pricing.js').then(async (m) => {
  await m.refreshBtcContext({ btcMints: [] });
  console.log(m.getBtcContext());
});" 2>/dev/null || true
# Альтернатива (без сборки):
npm run papertrader -- &
PID=$!
sleep 5
kill $PID
# в /tmp/paper-trades.jsonl последняя строка должна показать btc.ret1h_pct числом
```

## Шаг 2 — Расширить `src/papertrader/config.ts`

Добавить **в зод-схему** (ниже существующих полей, перед закрывающей `})`):

```typescript
  // ---- discovery lanes (W6.3b) ----
  enableLaunchpadLane: z.boolean().default(false),  // Smart Lottery — stub в W6.3b, реальная реализация — W6.7
  enableMigrationLane: z.boolean().default(true),
  enablePostLane: z.boolean().default(true),

  // ---- discovery window (legacy launchpad) ----
  decisionAgeMin: z.coerce.number().int().min(1).max(120).default(7),
  decisionAgeMaxMin: z.coerce.number().int().min(1).max(360).default(12),
  windowStartMin: z.coerce.number().int().min(0).max(60).default(2),
  bcGraduationSol: z.coerce.number().positive().default(85),

  // ---- global gate ----
  globalMinTokenAgeMin: z.coerce.number().nonnegative().default(0),
  globalMinHolderCount: z.coerce.number().int().nonnegative().default(0),

  // ---- snapshot lanes ----
  laneMigMinLiqUsd: z.coerce.number().nonnegative().default(12_000),
  laneMigMinVol5mUsd: z.coerce.number().nonnegative().default(1_800),
  laneMigMinBuys5m: z.coerce.number().int().nonnegative().default(18),
  laneMigMinSells5m: z.coerce.number().int().nonnegative().default(8),
  laneMigMinAgeMin: z.coerce.number().nonnegative().default(2),
  laneMigMaxAgeMin: z.coerce.number().nonnegative().default(25),
  lanePostMinLiqUsd: z.coerce.number().nonnegative().default(15_000),
  lanePostMinVol5mUsd: z.coerce.number().nonnegative().default(2_500),
  lanePostMinBuys5m: z.coerce.number().int().nonnegative().default(16),
  lanePostMinSells5m: z.coerce.number().int().nonnegative().default(10),
  lanePostMinAgeMin: z.coerce.number().nonnegative().default(25),
  lanePostMaxAgeMin: z.coerce.number().nonnegative().default(180),
  snapshotMinBs: z.coerce.number().nonnegative().default(1.0),

  // ---- dip detector ----
  dipLookbackMin: z.coerce.number().int().positive().default(60),
  dipMinDropPct: z.coerce.number().default(-12),
  dipMaxDropPct: z.coerce.number().default(-45),
  dipMinImpulsePct: z.coerce.number().default(20),
  dipMinAgeMin: z.coerce.number().nonnegative().default(25),
  dipCooldownMinDefault: z.coerce.number().nonnegative().default(120),
  dipCooldownMinScalp: z.coerce.number().nonnegative().default(20),

  // ---- whale analysis ----
  whaleEnabled: z.boolean().default(false),
  whaleRequireTrigger: z.boolean().default(false),
  whaleLargeSellUsd: z.coerce.number().nonnegative().default(3_000),
  whaleRecentLookbackMin: z.coerce.number().nonnegative().default(10),
  whaleCapitulationPct: z.coerce.number().min(0).max(1).default(0.7),
  whaleGroupSellUsd: z.coerce.number().nonnegative().default(5_000),
  whaleGroupMinSellers: z.coerce.number().int().nonnegative().default(2),
  whaleGroupDumpPct: z.coerce.number().min(0).max(1).default(0.4),
  whaleBlockCreatorDump: z.boolean().default(true),
  whaleCreatorDumpLookbackMin: z.coerce.number().nonnegative().default(20),
  whaleCreatorDumpMinPct: z.coerce.number().min(0).max(1).default(0.05),
  whaleCreatorDumpMaxPct: z.coerce.number().min(0).max(1).default(0.6),
  whaleDcaPredMinSells24h: z.coerce.number().int().nonnegative().default(4),
  whaleDcaPredMinIntervalMin: z.coerce.number().nonnegative().default(30),
  whaleDcaPredMinChunkUsd: z.coerce.number().nonnegative().default(3_000),
  whaleDcaAggrMinSells24h: z.coerce.number().int().nonnegative().default(6),
  whaleDcaAggrMaxIntervalMin: z.coerce.number().nonnegative().default(15),

  // ---- legacy launchpad filters (used only when enableLaunchpadLane=true; in W6.3b stub-blocked) ----
  filtMinUniqueBuyers: z.coerce.number().int().nonnegative().default(20),
  filtMinBuySol: z.coerce.number().nonnegative().default(5),
  filtMinBuySellRatio: z.coerce.number().nonnegative().default(1.5),
  filtMaxTopBuyerShare: z.coerce.number().min(0).max(1).default(0.35),
  filtMinBcProgress: z.coerce.number().min(0).max(1).default(0.25),
  filtMaxBcProgress: z.coerce.number().min(0).max(1).default(0.95),
```

И **в `loadPaperTraderConfig`** добавить парсинг (нижний раздел, после уже существующих ключей):

```typescript
    enableLaunchpadLane: envBool(process.env.PAPER_ENABLE_LAUNCHPAD_LANE, false),
    enableMigrationLane: envBool(process.env.PAPER_ENABLE_MIGRATION_LANE, true),
    enablePostLane: envBool(process.env.PAPER_ENABLE_POST_LANE, true),
    decisionAgeMin: process.env.PAPER_DECISION_AGE_MIN,
    decisionAgeMaxMin: process.env.PAPER_DECISION_AGE_MAX_MIN,
    windowStartMin: process.env.PAPER_WINDOW_START_MIN,
    bcGraduationSol: process.env.PAPER_BC_GRADUATION_SOL,
    globalMinTokenAgeMin: process.env.PAPER_MIN_TOKEN_AGE_MIN,
    globalMinHolderCount: process.env.PAPER_MIN_HOLDER_COUNT,
    laneMigMinLiqUsd: process.env.PAPER_MIG_MIN_LIQ_USD,
    laneMigMinVol5mUsd: process.env.PAPER_MIG_MIN_VOL_5M_USD,
    laneMigMinBuys5m: process.env.PAPER_MIG_MIN_BUYS_5M,
    laneMigMinSells5m: process.env.PAPER_MIG_MIN_SELLS_5M,
    laneMigMinAgeMin: process.env.PAPER_MIG_MIN_AGE_MIN,
    laneMigMaxAgeMin: process.env.PAPER_MIG_MAX_AGE_MIN,
    lanePostMinLiqUsd: process.env.PAPER_POST_MIN_LIQ_USD,
    lanePostMinVol5mUsd: process.env.PAPER_POST_MIN_VOL_5M_USD,
    lanePostMinBuys5m: process.env.PAPER_POST_MIN_BUYS_5M,
    lanePostMinSells5m: process.env.PAPER_POST_MIN_SELLS_5M,
    lanePostMinAgeMin: process.env.PAPER_POST_MIN_AGE_MIN,
    lanePostMaxAgeMin: process.env.PAPER_POST_MAX_AGE_MIN,
    snapshotMinBs: process.env.PAPER_POST_MIN_BS,
    dipLookbackMin: process.env.PAPER_DIP_LOOKBACK_MIN,
    dipMinDropPct: process.env.PAPER_DIP_MIN_DROP_PCT,
    dipMaxDropPct: process.env.PAPER_DIP_MAX_DROP_PCT,
    dipMinImpulsePct: process.env.PAPER_DIP_MIN_IMPULSE_PCT,
    dipMinAgeMin: process.env.PAPER_DIP_MIN_AGE_MIN,
    dipCooldownMinDefault: process.env.PAPER_DIP_COOLDOWN_MIN,
    dipCooldownMinScalp: process.env.PAPER_DIP_COOLDOWN_MIN_SCALP,
    whaleEnabled: envBool(process.env.PAPER_DIP_WHALE_ANALYSIS_ENABLED, false),
    whaleRequireTrigger: envBool(process.env.PAPER_DIP_REQUIRE_WHALE_TRIGGER, false),
    whaleLargeSellUsd: process.env.PAPER_DIP_LARGE_SELL_USD,
    whaleRecentLookbackMin: process.env.PAPER_DIP_RECENT_LOOKBACK_MIN,
    whaleCapitulationPct: process.env.PAPER_DIP_CAPITULATION_PCT,
    whaleGroupSellUsd: process.env.PAPER_DIP_GROUP_SELL_USD,
    whaleGroupMinSellers: process.env.PAPER_DIP_GROUP_MIN_SELLERS,
    whaleGroupDumpPct: process.env.PAPER_DIP_GROUP_DUMP_PCT,
    whaleBlockCreatorDump: envBool(process.env.PAPER_DIP_BLOCK_CREATOR_DUMP, true),
    whaleCreatorDumpLookbackMin: process.env.PAPER_DIP_CREATOR_DUMP_LOOKBACK_MIN,
    whaleCreatorDumpMinPct: process.env.PAPER_DIP_CREATOR_DUMP_MIN_PCT,
    whaleCreatorDumpMaxPct: process.env.PAPER_DIP_CREATOR_DUMP_MAX_PCT,
    whaleDcaPredMinSells24h: process.env.PAPER_DIP_DCA_PRED_MIN_SELLS_24H,
    whaleDcaPredMinIntervalMin: process.env.PAPER_DIP_DCA_PRED_MIN_INTERVAL_MIN,
    whaleDcaPredMinChunkUsd: process.env.PAPER_DIP_DCA_PRED_MIN_CHUNK_USD,
    whaleDcaAggrMinSells24h: process.env.PAPER_DIP_DCA_AGGR_MIN_SELLS_24H,
    whaleDcaAggrMaxIntervalMin: process.env.PAPER_DIP_DCA_AGGR_MAX_INTERVAL_MIN,
    filtMinUniqueBuyers: process.env.PAPER_MIN_UNIQUE_BUYERS,
    filtMinBuySol: process.env.PAPER_MIN_BUY_SOL,
    filtMinBuySellRatio: process.env.PAPER_MIN_BUY_SELL_RATIO,
    filtMaxTopBuyerShare: process.env.PAPER_MAX_TOP_BUYER_SHARE,
    filtMinBcProgress: process.env.PAPER_MIN_BC_PROGRESS,
    filtMaxBcProgress: process.env.PAPER_MAX_BC_PROGRESS,
```

## Шаг 3 — Расширить `src/papertrader/types.ts`

Добавить **в конец файла** (после `HeartbeatEvent`):

```typescript
// ---- discovery (W6.3b) ----
export interface SnapshotCandidateRow {
  mint: string;
  symbol: string;
  ts: Date | string;
  launch_ts: Date | string | null;
  age_min: number | null;
  price_usd: number;
  liquidity_usd: number;
  volume_5m: number;
  buys_5m: number;
  sells_5m: number;
  market_cap_usd: number | null;
  source: string;        // 'raydium' | 'meteora' | 'orca' | 'moonshot'
  holder_count: number;  // 0 placeholder until W6.7 enrichment
  token_age_min: number;
}

export interface DipContext {
  high_px: number;
  low_px: number;
}

export interface SnapshotFeatures {
  price_usd: number;
  liq_usd: number;
  vol5m_usd: number;
  buys5m: number;
  sells5m: number;
  buy_sell_ratio_5m: number | null;
  holders: number;
  token_age_min: number;
  dip_pct: number | null;
  impulse_pct: number | null;
}

// ---- whale-analysis (W6.3b) ----
export type SellerProfile =
  | 'capitulator'
  | 'dca_predictable'
  | 'dca_aggressive'
  | 'panic_random'
  | 'unknown';

export interface WhaleSeller {
  wallet: string;
  amount_usd: number;
  pct_of_position_dumped: number;
  pct_total_dumped_now: number;
  is_creator: boolean;
  profile: SellerProfile;
  n_sells_24h: number;
  median_interval_min: number | null;
  median_chunk_usd: number | null;
}

export interface WhaleAnalysis {
  enabled: boolean;
  creator_wallet: string | null;
  creator_dumped_pct: number;
  creator_dump_block: boolean;
  large_sells: WhaleSeller[];
  single_whale_capitulation: boolean;
  group_sell_pressure: boolean;
  dca_predictable_present: boolean;
  dca_aggressive_present: boolean;
  trigger_fired: 'whale_capitulation' | 'group_pressure' | 'dca_predictable' | null;
  block_reasons: string[];
}

// ---- eval JSONL events (W6.3b) ----
export interface EvalEvent extends JsonlEventBase {
  kind: 'eval';
  lane: Lane;
  source?: string;
  mint: string;
  symbol: string;
  ageMin: number;
  pass: boolean;
  reasons: string[];
  m: SnapshotFeatures;
  btc: { ret1h_pct: number | null; ret4h_pct: number | null; updated_ts: number | null };
  whale_analysis: WhaleAnalysis | null;
}

export interface EvalSkipOpenEvent extends JsonlEventBase {
  kind: 'eval-skip-open';
  lane: Lane;
  source?: string;
  mint: string;
  reason: string;
}
```

## Шаг 4 — Создать модули discovery / filters / dip / whale

### 4.1. `src/papertrader/filters/global-gate.ts`

```typescript
import type { PaperTraderConfig } from '../config.js';

export function globalGate(
  cfg: PaperTraderConfig,
  tokenAgeMin?: number | null,
  holderCount?: number | null,
): string[] {
  const reasons: string[] = [];
  const age = Number(tokenAgeMin ?? 0);
  const holders = Number(holderCount ?? 0);
  if (cfg.globalMinTokenAgeMin > 0 && age < cfg.globalMinTokenAgeMin) {
    reasons.push(`token_age<${cfg.globalMinTokenAgeMin}m`);
  }
  if (cfg.globalMinHolderCount > 0 && holders < cfg.globalMinHolderCount) {
    reasons.push(`holders<${cfg.globalMinHolderCount}`);
  }
  return reasons;
}
```

### 4.2. `src/papertrader/filters/snapshot-filter.ts`

```typescript
import type { PaperTraderConfig } from '../config.js';
import type { Lane, SnapshotCandidateRow } from '../types.js';

interface LaneCfg {
  MIN_LIQ_USD: number;
  MIN_VOL_5M_USD: number;
  MIN_BUYS_5M: number;
  MIN_SELLS_5M: number;
  MIN_AGE_MIN: number;
  MAX_AGE_MIN: number;
}

export function laneCfg(cfg: PaperTraderConfig, lane: Lane): LaneCfg {
  if (lane === 'migration_event') {
    return {
      MIN_LIQ_USD: cfg.laneMigMinLiqUsd,
      MIN_VOL_5M_USD: cfg.laneMigMinVol5mUsd,
      MIN_BUYS_5M: cfg.laneMigMinBuys5m,
      MIN_SELLS_5M: cfg.laneMigMinSells5m,
      MIN_AGE_MIN: cfg.laneMigMinAgeMin,
      MAX_AGE_MIN: cfg.laneMigMaxAgeMin,
    };
  }
  return {
    MIN_LIQ_USD: cfg.lanePostMinLiqUsd,
    MIN_VOL_5M_USD: cfg.lanePostMinVol5mUsd,
    MIN_BUYS_5M: cfg.lanePostMinBuys5m,
    MIN_SELLS_5M: cfg.lanePostMinSells5m,
    MIN_AGE_MIN: cfg.lanePostMinAgeMin,
    MAX_AGE_MIN: cfg.lanePostMaxAgeMin,
  };
}

export function evaluateSnapshot(
  cfg: PaperTraderConfig,
  row: SnapshotCandidateRow,
  lane: Lane,
): { pass: boolean; reasons: string[] } {
  const lc = laneCfg(cfg, lane);
  const reasons: string[] = [];
  if (row.liquidity_usd < lc.MIN_LIQ_USD) reasons.push(`liq<${lc.MIN_LIQ_USD}`);
  if (row.volume_5m < lc.MIN_VOL_5M_USD) reasons.push(`vol5m<${lc.MIN_VOL_5M_USD}`);
  if (row.buys_5m < lc.MIN_BUYS_5M) reasons.push(`buys5m<${lc.MIN_BUYS_5M}`);
  if (row.sells_5m < lc.MIN_SELLS_5M) reasons.push(`sells5m<${lc.MIN_SELLS_5M}`);
  const bs = row.sells_5m > 0 ? row.buys_5m / row.sells_5m : row.buys_5m;
  if (bs < cfg.snapshotMinBs) reasons.push(`bs<${cfg.snapshotMinBs}`);
  return { pass: reasons.length === 0, reasons };
}
```

### 4.3. `src/papertrader/dip-detector.ts`

```typescript
import { sql as dsql } from 'drizzle-orm';
import { db } from '../core/db/client.js';
import type { PaperTraderConfig } from './config.js';
import type { DipContext, SnapshotCandidateRow } from './types.js';

function sourceSnapshotTable(source: string): string | null {
  if (source === 'raydium') return 'raydium_pair_snapshots';
  if (source === 'meteora') return 'meteora_pair_snapshots';
  if (source === 'orca') return 'orca_pair_snapshots';
  if (source === 'moonshot') return 'moonshot_pair_snapshots';
  return null;
}

function sqlQuote(value: string): string {
  return `'${value.replace(/'/g, "''")}'`;
}

export async function fetchDipContextMap(
  cfg: PaperTraderConfig,
  rows: SnapshotCandidateRow[],
): Promise<Map<string, DipContext>> {
  const map = new Map<string, DipContext>();
  const byTable = new Map<string, string[]>();
  for (const r of rows) {
    const t = sourceSnapshotTable(r.source);
    if (!t) continue;
    const arr = byTable.get(t) ?? [];
    arr.push(r.mint);
    byTable.set(t, arr);
  }

  for (const [table, mintsRaw] of byTable.entries()) {
    const uniq = [...new Set(mintsRaw)];
    if (!uniq.length) continue;
    const mintsSql = uniq.map(sqlQuote).join(',');
    const r = await db.execute(dsql.raw(`
      SELECT
        base_mint AS mint,
        MAX(COALESCE(price_usd, 0))::float AS high_px,
        MIN(NULLIF(COALESCE(price_usd, 0), 0))::float AS low_px
      FROM ${table}
      WHERE ts >= now() - interval '${cfg.dipLookbackMin} minutes'
        AND base_mint IN (${mintsSql})
      GROUP BY base_mint
    `));
    const out = r as unknown as Array<{ mint: string; high_px: number | string; low_px: number | string }>;
    for (const row of out) {
      map.set(String(row.mint), {
        high_px: Number(row.high_px || 0),
        low_px: Number(row.low_px || 0),
      });
    }
  }
  return map;
}

export function evaluateDip(
  cfg: PaperTraderConfig,
  row: SnapshotCandidateRow,
  ctx?: DipContext | null,
): { reasons: string[]; dipPct: number | null; impulsePct: number | null } {
  const reasons: string[] = [];
  if ((row.token_age_min ?? 0) < cfg.dipMinAgeMin) reasons.push(`dip_age<${cfg.dipMinAgeMin}m`);
  if (!ctx || !(ctx.high_px > 0)) {
    return { reasons: [...reasons, 'dip_ctx_missing'], dipPct: null, impulsePct: null };
  }
  const dipPct = (row.price_usd / ctx.high_px - 1) * 100;
  if (dipPct > cfg.dipMinDropPct) reasons.push(`dip_not_deep_enough>${cfg.dipMinDropPct}%`);
  if (dipPct < cfg.dipMaxDropPct) reasons.push(`dip_too_deep<${cfg.dipMaxDropPct}%`);
  const impulsePct = ctx.low_px > 0 ? (ctx.high_px / ctx.low_px - 1) * 100 : null;
  if ((impulsePct ?? 0) < cfg.dipMinImpulsePct) reasons.push(`impulse<${cfg.dipMinImpulsePct}%`);
  return { reasons, dipPct, impulsePct };
}
```

### 4.4. `src/papertrader/whale-analysis.ts`

Полный 1-в-1 порт `fetchWhaleAnalysis` из salvage (`live-paper-trader.ts:770-968`), но typed и использующий `PaperTraderConfig` вместо глобального `WHALE`-объекта. Скопируй структуру из salvage, замени `WHALE.X` на `cfg.whaleX`. Источник для копирования — `_salvage_legacy/scripts-tmp/live-paper-trader.ts:740-968` (функции `classifyProfile`, `median`, `fetchWhaleAnalysis`).

Сигнатура:

```typescript
export async function fetchWhaleAnalysis(
  cfg: PaperTraderConfig,
  mint: string,
): Promise<WhaleAnalysis> { ... }
```

Если `cfg.whaleEnabled === false` — возврат `empty` (см. salvage:771-783) **сразу**, без SQL. Это критично — на ранней итерации W6.3b мы НЕ включаем whale (нагрузка на БД), включит W6.5 при запуске Oscar/Dno.

`shortWallet`, `classifyProfile`, `median`, типы — также скопировать из salvage, превратив `WHALE.*` → `cfg.whale*`.

### 4.5. `src/papertrader/discovery/snapshot.ts`

```typescript
import { sql as dsql } from 'drizzle-orm';
import { db } from '../../core/db/client.js';
import type { PaperTraderConfig } from '../config.js';
import type { Lane, SnapshotCandidateRow } from '../types.js';
import { laneCfg } from '../filters/snapshot-filter.js';

const SNAPSHOT_TABLES: Array<{ table: string; source: string }> = [
  { table: 'raydium_pair_snapshots', source: 'raydium' },
  { table: 'meteora_pair_snapshots', source: 'meteora' },
  { table: 'orca_pair_snapshots', source: 'orca' },
  { table: 'moonshot_pair_snapshots', source: 'moonshot' },
];

export async function fetchSnapshotLaneCandidates(
  cfg: PaperTraderConfig,
  lane: Lane,
): Promise<SnapshotCandidateRow[]> {
  const lc = laneCfg(cfg, lane);
  const unions = SNAPSHOT_TABLES.map(
    (t) => `
    SELECT
      p.base_mint AS mint,
      COALESCE(tok.symbol, '?') AS symbol,
      0::int AS holder_count,
      EXTRACT(EPOCH FROM (now() - COALESCE(tok.first_seen_at, p.ts))) / 60.0 AS token_age_min,
      p.ts,
      NULL::timestamptz AS launch_ts,
      EXTRACT(EPOCH FROM (p.ts - COALESCE(tok.first_seen_at, p.ts))) / 60.0 AS age_min,
      COALESCE(p.price_usd, 0)::float AS price_usd,
      COALESCE(p.liquidity_usd, 0)::float AS liquidity_usd,
      COALESCE(p.volume_5m, 0)::float AS volume_5m,
      COALESCE(p.buys_5m, 0)::int AS buys_5m,
      COALESCE(p.sells_5m, 0)::int AS sells_5m,
      COALESCE(p.market_cap_usd, p.fdv_usd, 0)::float AS market_cap_usd,
      '${t.source}'::text AS source
    FROM ${t.table} p
    LEFT JOIN tokens tok ON tok.mint = p.base_mint
    WHERE p.ts >= now() - interval '30 minutes'
      AND COALESCE(p.price_usd, 0) > 0
  `,
  ).join('\nUNION ALL\n');

  const maxAgeFilter = lc.MAX_AGE_MIN > 0 ? `AND COALESCE(age_min, 0) <= ${lc.MAX_AGE_MIN}` : '';

  const r = await db.execute(dsql.raw(`
    WITH raw AS (
      ${unions}
    ),
    ranked AS (
      SELECT *,
             ROW_NUMBER() OVER (PARTITION BY mint ORDER BY ts DESC) AS rn
      FROM raw
    )
    SELECT *
    FROM ranked
    WHERE rn = 1
      AND COALESCE(age_min, 0) >= ${lc.MIN_AGE_MIN}
      ${maxAgeFilter}
      AND liquidity_usd >= ${lc.MIN_LIQ_USD}
      AND volume_5m >= ${lc.MIN_VOL_5M_USD}
      AND buys_5m >= ${lc.MIN_BUYS_5M}
      AND sells_5m >= ${lc.MIN_SELLS_5M}
    ORDER BY ts DESC
    LIMIT 300
  `));
  return r as unknown as SnapshotCandidateRow[];
}
```

### 4.6. `src/papertrader/discovery/launchpad.ts` (stub)

```typescript
import type { PaperTraderConfig } from '../config.js';

export async function fetchLaunchpadCandidates(_cfg: PaperTraderConfig): Promise<never[]> {
  // Implemented in W6.7 (Smart Lottery — uses entity_wallets.primary_tag).
  return [];
}
```

### 4.7. `src/papertrader/discovery/fresh-validated.ts` (stub)

```typescript
import type { PaperTraderConfig } from '../config.js';

export async function fetchFreshValidatedCandidates(_cfg: PaperTraderConfig): Promise<never[]> {
  // Implemented in W6.7 (FV-lane — uses tokens.metadata + holders enrichment).
  return [];
}
```

### 4.8. `src/papertrader/discovery/dip-clones.ts`

Этот модуль — **главный orchestrator** discovery-tick для STRATEGY_KIND=dip. Он сам **не пишет в JSONL** (это делает `main.ts`), а возвращает массив `EvalDecision`, которые `main.ts` логирует.

```typescript
import type { PaperTraderConfig } from '../config.js';
import type { Lane, SnapshotCandidateRow, SnapshotFeatures, WhaleAnalysis } from '../types.js';
import { fetchSnapshotLaneCandidates } from './snapshot.js';
import { evaluateSnapshot } from '../filters/snapshot-filter.js';
import { globalGate } from '../filters/global-gate.js';
import { fetchDipContextMap, evaluateDip } from '../dip-detector.js';
import { fetchWhaleAnalysis } from '../whale-analysis.js';

export interface EvalDecision {
  lane: Lane;
  source: string;
  mint: string;
  symbol: string;
  ageMin: number;
  pass: boolean;
  reasons: string[];
  features: SnapshotFeatures;
  whale: WhaleAnalysis | null;
}

export interface DiscoveryTickResult {
  discovered: number;
  evaluated: number;
  passed: number;
  decisions: EvalDecision[];
}

const evaluatedAt = new Map<string, number>(); // mint -> last eval ts
const lastEntryTsByMint = new Map<string, number>(); // mint -> last open ts (W6.3c will set this; W6.3b reads only)

function shouldEvaluate(mint: string, reevalAfterSec: number): boolean {
  const last = evaluatedAt.get(mint) || 0;
  if (Date.now() - last < reevalAfterSec * 1000) return false;
  evaluatedAt.set(mint, Date.now());
  return true;
}

function buildFeatures(
  row: SnapshotCandidateRow,
  dipPct: number | null,
  impulsePct: number | null,
): SnapshotFeatures {
  return {
    price_usd: +Number(row.price_usd || 0).toFixed(8),
    liq_usd: +Number(row.liquidity_usd || 0).toFixed(0),
    vol5m_usd: +Number(row.volume_5m || 0).toFixed(0),
    buys5m: row.buys_5m,
    sells5m: row.sells_5m,
    buy_sell_ratio_5m: row.sells_5m > 0 ? +(row.buys_5m / row.sells_5m).toFixed(2) : null,
    holders: row.holder_count,
    token_age_min: +Number(row.token_age_min ?? 0).toFixed(1),
    dip_pct: dipPct !== null ? +dipPct.toFixed(2) : null,
    impulse_pct: impulsePct !== null ? +impulsePct.toFixed(2) : null,
  };
}

export async function runDipDiscovery(cfg: PaperTraderConfig): Promise<DiscoveryTickResult> {
  // W6.3b: STRATEGY_KIND=dip pulls both MIGRATION + POST snapshot lanes.
  const [migRows, postRows] = await Promise.all([
    cfg.enableMigrationLane ? fetchSnapshotLaneCandidates(cfg, 'migration_event') : Promise.resolve([]),
    cfg.enablePostLane ? fetchSnapshotLaneCandidates(cfg, 'post_migration') : Promise.resolve([]),
  ]);
  const snapshotRows = [...migRows, ...postRows];
  if (snapshotRows.length === 0) {
    return { discovered: 0, evaluated: 0, passed: 0, decisions: [] };
  }
  const dipMap = await fetchDipContextMap(cfg, snapshotRows);
  const reevalAfterSec = 60; // STRATEGY_KIND=dip → REEVAL_AFTER_SEC=60 (legacy)

  const decisions: EvalDecision[] = [];
  let evaluated = 0;
  let passed = 0;

  for (const row of snapshotRows) {
    const lane: Lane = migRows.includes(row) ? 'migration_event' : 'post_migration';
    if (!shouldEvaluate(row.mint, reevalAfterSec)) continue;
    evaluated++;

    const v = evaluateSnapshot(cfg, row, lane);
    const globalReasons = globalGate(cfg, row.token_age_min, row.holder_count);
    const dipEval = evaluateDip(cfg, row, dipMap.get(row.mint));
    const baseReasons = [...v.reasons, ...globalReasons, ...dipEval.reasons];
    const baseDipPass = baseReasons.length === 0;

    let whale: WhaleAnalysis | null = null;
    const whaleReasons: string[] = [];
    if (baseDipPass && cfg.whaleEnabled) {
      whale = await fetchWhaleAnalysis(cfg, row.mint);
      if (whale.creator_dump_block) {
        whaleReasons.push(`creator_dumping_${(whale.creator_dumped_pct * 100).toFixed(0)}%`);
      }
      if (whale.dca_aggressive_present) whaleReasons.push('dca_aggressive_seller');
      if (cfg.whaleRequireTrigger && !whale.trigger_fired && !whaleReasons.length) {
        whaleReasons.push('no_whale_trigger');
      }
    }

    const cooldownMin =
      whale?.trigger_fired === 'dca_predictable' ? cfg.dipCooldownMinScalp : cfg.dipCooldownMinDefault;
    const lastEntry = lastEntryTsByMint.get(row.mint) || 0;
    const minutesSinceLast = (Date.now() - lastEntry) / 60_000;
    const cooldownReasons: string[] = [];
    if (lastEntry > 0 && minutesSinceLast < cooldownMin) {
      cooldownReasons.push(
        `cooldown_active_${cooldownMin}m_left_${(cooldownMin - minutesSinceLast).toFixed(0)}m`,
      );
    }

    const mergedReasons = [...baseReasons, ...whaleReasons, ...cooldownReasons];
    const pass = mergedReasons.length === 0;
    if (pass) passed++;

    decisions.push({
      lane,
      source: row.source,
      mint: row.mint,
      symbol: row.symbol,
      ageMin: +Number(row.age_min ?? 0).toFixed(1),
      pass,
      reasons: mergedReasons,
      features: buildFeatures(row, dipEval.dipPct, dipEval.impulsePct),
      whale,
    });
  }

  return { discovered: snapshotRows.length, evaluated, passed, decisions };
}
```

## Шаг 5 — Заменить `src/papertrader/main.ts`

```typescript
import pino from 'pino';
import { loadPaperTraderConfig } from './config.js';
import { configureStore, appendEvent } from './store-jsonl.js';
import { refreshSolPrice, getSolUsd, refreshBtcContext, getBtcContext } from './pricing.js';
import { runDipDiscovery } from './discovery/dip-clones.js';
import { fetchLaunchpadCandidates } from './discovery/launchpad.js';
import { fetchFreshValidatedCandidates } from './discovery/fresh-validated.js';

const logger = pino({ name: 'papertrader' });

export async function main(): Promise<void> {
  const cfg = loadPaperTraderConfig();
  configureStore({ storePath: cfg.storePath, strategyId: cfg.strategyId });

  // Touch stub modules so eslint's noUnusedLocals doesn't kill the build,
  // and so the runtime visibly logs which strategy is active.
  void fetchLaunchpadCandidates;
  void fetchFreshValidatedCandidates;

  const startedAt = Date.now();
  const stats = { discovered: 0, evaluated: 0, passed: 0, ticks: 0, errors: 0 };

  logger.info({
    msg: 'papertrader discovery start',
    strategyId: cfg.strategyId,
    strategyKind: cfg.strategyKind,
    storePath: cfg.storePath,
    positionUsd: cfg.positionUsd,
    dryRun: cfg.dryRun,
    whaleEnabled: cfg.whaleEnabled,
    enableMigrationLane: cfg.enableMigrationLane,
    enablePostLane: cfg.enablePostLane,
  });

  await Promise.allSettled([refreshSolPrice(), refreshBtcContext(cfg)]);

  // Discovery tick: only STRATEGY_KIND=dip is implemented in W6.3b.
  // For 'fresh' / 'smart_lottery' / 'fresh_validated' we log a heartbeat with note,
  // because launchpad / FV are stubs until W6.7.
  async function discoveryTick(): Promise<void> {
    stats.ticks++;
    try {
      if (cfg.strategyKind !== 'dip') return; // no-op until W6.7
      const res = await runDipDiscovery(cfg);
      stats.discovered += res.discovered;
      stats.evaluated += res.evaluated;
      stats.passed += res.passed;
      const btc = getBtcContext();
      for (const d of res.decisions) {
        appendEvent({
          kind: 'eval',
          lane: d.lane,
          source: d.source,
          mint: d.mint,
          symbol: d.symbol,
          ageMin: d.ageMin,
          pass: d.pass,
          reasons: d.reasons,
          m: d.features,
          btc,
          whale_analysis: d.whale,
        });
      }
    } catch (err) {
      stats.errors++;
      logger.warn({ msg: 'discovery tick failed', err: (err as Error).message });
    }
  }

  const discoveryTimer = setInterval(() => {
    void discoveryTick();
  }, cfg.discoveryIntervalMs);

  const heartbeatTimer = setInterval(() => {
    const note =
      cfg.strategyKind !== 'dip'
        ? `strategy_kind=${cfg.strategyKind} not implemented in W6.3b (stubbed)`
        : `discovery active: ticks=${stats.ticks} discovered=${stats.discovered} evaluated=${stats.evaluated} passed=${stats.passed} errors=${stats.errors}`;
    appendEvent({
      kind: 'heartbeat',
      uptimeSec: Math.round((Date.now() - startedAt) / 1000),
      openPositions: 0,
      closedTotal: 0,
      solUsd: getSolUsd(),
      btc: getBtcContext(),
      note,
    });
    logger.info({ msg: 'heartbeat', solUsd: getSolUsd(), btc: getBtcContext(), stats, note });
  }, cfg.heartbeatIntervalMs);

  const solTimer = setInterval(() => { void refreshSolPrice(); }, cfg.solPriceRefreshMs);
  const btcTimer = setInterval(() => { void refreshBtcContext(cfg); }, cfg.btcContextRefreshMs);

  // Run one discovery tick immediately so we don't wait full discoveryIntervalMs.
  await discoveryTick();

  const shutdown = (sig: string) => {
    logger.info({ msg: 'papertrader shutdown', sig, stats });
    clearInterval(discoveryTimer);
    clearInterval(heartbeatTimer);
    clearInterval(solTimer);
    clearInterval(btcTimer);
    setTimeout(() => process.exit(0), 200);
  };
  process.on('SIGINT', () => shutdown('SIGINT'));
  process.on('SIGTERM', () => shutdown('SIGTERM'));
}
```

## Шаг 6 — `ecosystem.config.cjs` — заменить `pt1-skeleton-test` на `pt1-dip-discovery-test`

Найти в `apps:` блок `pt1-skeleton-test` (последний в массиве после `sa-direct-lp`) и **заменить целиком**:

```javascript
{
  name: 'pt1-dip-discovery-test',
  cwd: root,
  script: 'npm',
  args: 'run --silent papertrader',
  interpreter: 'none',
  exec_mode: 'fork',
  instances: 1,
  autorestart: true,
  max_restarts: 30,
  restart_delay: 5000,
  max_memory_restart: '350M',
  merge_logs: true,
  time: true,
  env: {
    NODE_ENV: 'production',
    PAPER_STRATEGY_ID: 'pt1-dip-discovery-test',
    PAPER_STRATEGY_KIND: 'dip',
    PAPER_TRADES_PATH: path.join(root, 'data/paper2/pt1-dip-discovery-test.jsonl'),
    PAPER_HEARTBEAT_INTERVAL_MS: '30000',
    PAPER_DISCOVERY_INTERVAL_MS: '15000',
    PAPER_DRY_RUN: 'true',
    PAPER_DIP_WHALE_ANALYSIS_ENABLED: '0',  // включается в W6.5 для Oscar/Dno
    PAPER_ENABLE_MIGRATION_LANE: 'true',
    PAPER_ENABLE_POST_LANE: 'true',
  },
},
```

(Имя нового app — `pt1-dip-discovery-test`. Старый `pt1-skeleton-test` нужно будет на VPS отдельной командой `pm2 delete pt1-skeleton-test` — см. шаг 8.)

## Шаг 7 — `.env.example` — добавить блок W6.3b

В конец файла:

```env

# ============================================================
# W6.3b — discovery / filters / dip / whale
# ============================================================
PAPER_ENABLE_LAUNCHPAD_LANE=false   # Smart Lottery — реализуется в W6.7
PAPER_ENABLE_MIGRATION_LANE=true
PAPER_ENABLE_POST_LANE=true

# Snapshot lanes (legacy LANE_MIGRATION / LANE_POST defaults)
PAPER_MIG_MIN_LIQ_USD=12000
PAPER_MIG_MIN_VOL_5M_USD=1800
PAPER_MIG_MIN_BUYS_5M=18
PAPER_MIG_MIN_SELLS_5M=8
PAPER_MIG_MIN_AGE_MIN=2
PAPER_MIG_MAX_AGE_MIN=25
PAPER_POST_MIN_LIQ_USD=15000
PAPER_POST_MIN_VOL_5M_USD=2500
PAPER_POST_MIN_BUYS_5M=16
PAPER_POST_MIN_SELLS_5M=10
PAPER_POST_MIN_AGE_MIN=25
PAPER_POST_MAX_AGE_MIN=180
PAPER_POST_MIN_BS=1.0

# Global gate
PAPER_MIN_TOKEN_AGE_MIN=0
PAPER_MIN_HOLDER_COUNT=0

# Dip detector
PAPER_DIP_LOOKBACK_MIN=60
PAPER_DIP_MIN_DROP_PCT=-12
PAPER_DIP_MAX_DROP_PCT=-45
PAPER_DIP_MIN_IMPULSE_PCT=20
PAPER_DIP_MIN_AGE_MIN=25
PAPER_DIP_COOLDOWN_MIN=120
PAPER_DIP_COOLDOWN_MIN_SCALP=20

# Whale analysis (default OFF; вкл при запуске Oscar/Dno в W6.5)
PAPER_DIP_WHALE_ANALYSIS_ENABLED=0
PAPER_DIP_REQUIRE_WHALE_TRIGGER=0
PAPER_DIP_LARGE_SELL_USD=3000
PAPER_DIP_RECENT_LOOKBACK_MIN=10
PAPER_DIP_CAPITULATION_PCT=0.7
PAPER_DIP_GROUP_SELL_USD=5000
PAPER_DIP_GROUP_MIN_SELLERS=2
PAPER_DIP_GROUP_DUMP_PCT=0.4
PAPER_DIP_BLOCK_CREATOR_DUMP=true
PAPER_DIP_CREATOR_DUMP_LOOKBACK_MIN=20
PAPER_DIP_CREATOR_DUMP_MIN_PCT=0.05
PAPER_DIP_CREATOR_DUMP_MAX_PCT=0.6
PAPER_DIP_DCA_PRED_MIN_SELLS_24H=4
PAPER_DIP_DCA_PRED_MIN_INTERVAL_MIN=30
PAPER_DIP_DCA_PRED_MIN_CHUNK_USD=3000
PAPER_DIP_DCA_AGGR_MIN_SELLS_24H=6
PAPER_DIP_DCA_AGGR_MAX_INTERVAL_MIN=15

# Legacy launchpad filters (used only when PAPER_ENABLE_LAUNCHPAD_LANE=true; W6.3b stub returns [])
PAPER_MIN_UNIQUE_BUYERS=20
PAPER_MIN_BUY_SOL=5
PAPER_MIN_BUY_SELL_RATIO=1.5
PAPER_MAX_TOP_BUYER_SHARE=0.35
PAPER_MIN_BC_PROGRESS=0.25
PAPER_MAX_BC_PROGRESS=0.95
PAPER_DECISION_AGE_MIN=7
PAPER_DECISION_AGE_MAX_MIN=12
PAPER_WINDOW_START_MIN=2
PAPER_BC_GRADUATION_SOL=85
```

## Шаг 8 — Локальная проверка

```bash
cd c:/Users/cente/Ideas/solana-alpha
npm run typecheck
npm run lint
npm run build
npm run test
```

Все четыре — **OK**, без новых ошибок.

Затем — smoke без VPS (требует доступ к prod БД через `SA_PG_DSN`):

```bash
PAPER_TRADES_PATH=/tmp/pt1-w63b-smoke.jsonl PAPER_STRATEGY_ID=smoke PAPER_STRATEGY_KIND=dip PAPER_HEARTBEAT_INTERVAL_MS=10000 PAPER_DISCOVERY_INTERVAL_MS=10000 PAPER_DRY_RUN=true npm run papertrader
```

Через ~30 секунд:
- В stdout идут `discovery start`, `heartbeat` со `stats={discovered>0, evaluated>0, passed>=0}`, `btc={ret1h_pct: <число>, ...}`.
- `cat /tmp/pt1-w63b-smoke.jsonl | grep '"kind":"eval"' | head -1` → JSON с полями `lane`, `source`, `mint`, `pass`, `reasons[]`, `m.{liq_usd,vol5m_usd,...}`, `btc.ret1h_pct`, `whale_analysis: null`.
- `cat /tmp/pt1-w63b-smoke.jsonl | grep -c '"kind":"open"'` → **0** (executor не реализован).
- `cat /tmp/pt1-w63b-smoke.jsonl | grep -c '"kind":"heartbeat"'` → **≥1**.

Ctrl+C → `papertrader shutdown SIGINT`. Удали `/tmp/pt1-w63b-smoke.jsonl`.

## Шаг 9 — Commit

`git status` должен показывать **строго**:

```
modified:   .env.example
modified:   ecosystem.config.cjs
modified:   src/papertrader/config.ts
modified:   src/papertrader/main.ts
modified:   src/papertrader/pricing.ts
modified:   src/papertrader/types.ts
new file:   src/papertrader/dip-detector.ts
new file:   src/papertrader/discovery/dip-clones.ts
new file:   src/papertrader/discovery/fresh-validated.ts
new file:   src/papertrader/discovery/launchpad.ts
new file:   src/papertrader/discovery/snapshot.ts
new file:   src/papertrader/filters/global-gate.ts
new file:   src/papertrader/filters/snapshot-filter.ts
new file:   src/papertrader/whale-analysis.ts
```

(Никаких `data/paper2/*.jsonl`, никаких изменений `package.json` / `costs.ts` / `store-jsonl.ts` / `src/scripts/papertrader.ts`.)

```bash
git add src/papertrader/ ecosystem.config.cjs .env.example
git commit -m "feat(papertrader): W6.3b — discovery + filters + dip + whale + Binance BTC fix"
```

## Шаг 10 — Push + squash-merge в `v2`

```bash
git -c http.sslVerify=false push -u origin w6.3b-papertrader-discovery
git checkout v2
git merge --squash w6.3b-papertrader-discovery
git commit -m "feat(papertrader): W6.3b — discovery + filters + dip + whale + Binance BTC fix"
git -c http.sslVerify=false push origin v2
git push origin --delete w6.3b-papertrader-discovery
git branch -D w6.3b-papertrader-discovery
git rev-parse HEAD
```

Запомни SHA — пригодится для DoD (HEAD на VPS должен совпасть).

## Шаг 11 — Деплой на VPS

Хост `187.124.38.242`, ключ `c:/Users/cente/.ssh/botadmin_187_auto`, runtime user `salpha`, рабочий каталог `/opt/solana-alpha`.

### 11.1. Pull + npm install

```bash
ssh -i c:/Users/cente/.ssh/botadmin_187_auto root@187.124.38.242 "sudo -u salpha -H bash -lc 'cd /opt/solana-alpha && git fetch origin && git reset --hard origin/v2 && git rev-parse HEAD && npm install --omit=dev'"
```

HEAD должен совпасть с локальным.

### 11.2. Снести старый `pt1-skeleton-test`

```bash
ssh -i c:/Users/cente/.ssh/botadmin_187_auto root@187.124.38.242 "sudo -u salpha -H bash -lc 'pm2 delete pt1-skeleton-test || true; pm2 save'"
```

### 11.3. Запустить новый `pt1-dip-discovery-test`

```bash
ssh -i c:/Users/cente/.ssh/botadmin_187_auto root@187.124.38.242 "sudo -u salpha -H bash -lc 'cd /opt/solana-alpha && pm2 start ecosystem.config.cjs --only pt1-dip-discovery-test && pm2 save && pm2 list'"
```

Ожидание: **11** процессов в `online` (10 W6.0/W6.1 + новый `pt1-dip-discovery-test`).

### 11.4. Warmup ~90 секунд + DoD

```bash
sleep 90

# Скрипт verify (запускать на VPS, можно через scp + ssh):
cat > /tmp/_w63b_verify.sh <<'BASH'
#!/usr/bin/env bash
set -euo pipefail
cd /opt/solana-alpha
echo "HEAD=$(git rev-parse HEAD)"
sudo -u salpha -H bash -lc 'pm2 jlist' \
  | jq -r '.[] | select(.name=="pt1-dip-discovery-test") | "pt1 status=\(.pm2_env.status) restarts=\(.pm2_env.restart_time) mem_mb=\((.monit.memory/1024/1024)|floor)"'

P=/opt/solana-alpha/data/paper2/pt1-dip-discovery-test.jsonl
echo "jsonl_lines=$(wc -l < "$P" | tr -d ' ')"

node -e "
const fs=require('fs');
const lines=fs.readFileSync('$P','utf8').trim().split(/\n/).filter(Boolean);
const cutoff=Date.now()-60000;
let evals_60=0, hb_60=0, evals_pass=0, lastHb=null, lastEval=null, openOrClose=0;
for (const line of lines) {
  try {
    const j=JSON.parse(line);
    if (j.kind==='heartbeat'){ if (j.ts>=cutoff) hb_60++; lastHb=j; }
    if (j.kind==='eval'){ if (j.ts>=cutoff) evals_60++; if (j.pass) evals_pass++; lastEval=j; }
    if (['open','close','partial_sell','dca_add','peak','followup_snapshot'].includes(j.kind)) openOrClose++;
  } catch(e){}
}
console.log('hb_last60=' + hb_60);
console.log('evals_last60=' + evals_60);
console.log('evals_passed_total=' + evals_pass);
console.log('forbidden_kind_count=' + openOrClose);
console.log('btc_ret1h_pct=' + JSON.stringify(lastHb && lastHb.btc && lastHb.btc.ret1h_pct));
console.log('btc_ret4h_pct=' + JSON.stringify(lastHb && lastHb.btc && lastHb.btc.ret4h_pct));
console.log('solUsd=' + (lastHb && lastHb.solUsd));
if (lastEval) {
  console.log('sample_eval_lane=' + lastEval.lane + ' src=' + lastEval.source + ' pass=' + lastEval.pass + ' reasons=' + JSON.stringify((lastEval.reasons||[]).slice(0,3)));
}
"

# Регрессия
for u in /api/atlas/health /api/parser/health /api/stream/health /api/dex/raydium/health /api/jupiter/health /api/direct-lp/health; do
  printf "%-30s %s\n" "$u" "$(curl -sS -o /dev/null -w '%{http_code}' http://127.0.0.1:3008$u)"
done
printf "%-30s %s\n" "etonne" "$(curl -sS -o /dev/null -w '%{http_code}' https://etonne-moi.com/)"
BASH
chmod +x /tmp/_w63b_verify.sh
sudo -u salpha bash /tmp/_w63b_verify.sh
rm /tmp/_w63b_verify.sh
```

## DoD — все одновременно

1. ✅ `git rev-parse HEAD` на VPS == локальный `v2` HEAD после squash-merge.
2. ✅ `pm2 list` показывает **11** процессов (10 W6.0/W6.1 + `pt1-dip-discovery-test`), все `online`. Старого `pt1-skeleton-test` **нет**.
3. ✅ `pt1-dip-discovery-test` после 90 с: `status=online`, `restarts=0`, `mem_mb < 250`.
4. ✅ `data/paper2/pt1-dip-discovery-test.jsonl` существует, **`forbidden_kind_count == 0`** (никаких `open`/`close`/`peak`/...).
5. ✅ За последние 60 секунд: `hb_last60 ≥ 1` И `evals_last60 ≥ 1`.
6. ✅ В последнем heartbeat: `btc.ret1h_pct` — **число** (не null). `btc.ret4h_pct` — число (не null). `solUsd > 20`.
7. ✅ Хотя бы одно `eval` в JSONL имеет валидную структуру: `lane ∈ {migration_event, post_migration}`, `source ∈ {raydium, meteora, orca, moonshot}`, `pass ∈ {true, false}`, `reasons` — массив, `m.liq_usd`, `m.vol5m_usd`, `m.token_age_min` — числа.
8. ✅ Регрессия: все 6 health endpoints + etonne — HTTP **200**.

## Откат

```bash
ssh -i c:/Users/cente/.ssh/botadmin_187_auto root@187.124.38.242 "sudo -u salpha -H bash -lc '
  cd /opt/solana-alpha
  pm2 delete pt1-dip-discovery-test || true
  pm2 save
  git reset --hard 8a9de0eb267dee0dbc6d7148a4ec1786b60b9fac
  npm install --omit=dev
  pm2 start ecosystem.config.cjs --only pt1-skeleton-test
  pm2 save
'"

# локально
git revert <squash-commit-sha>
git -c http.sslVerify=false push origin v2
```

## После DoD

1. Обновить `docs/strategy/specs/INDEX.md`:
   - W6.3b: «**выполнено** ✓ (discovery + filters + dip + whale + Binance BTC, HEAD `<sha>`)»
   - W6.3c: «**следующая** (executor + main loop)».
2. Сообщить пользователю одной строкой: HEAD на VPS, `evals_last60`, `evals_passed_total`, `btc.ret1h_pct`, `forbidden_kind_count` (должен быть 0).

## Forbidden actions (повтор для надёжности)

- НЕ начинать W6.3c в этом же PR.
- НЕ создавать `executor/*`, `makeOpenTradeFromEntry`, `fetchPreEntryDynamics`, `fetchContextSwaps`, `schedulePendingFollowups`, `trackerTick`, `followupTick` — это W6.3c.
- НЕ модифицировать `costs.ts` / `store-jsonl.ts` / `src/scripts/papertrader.ts` / `package.json`.
- НЕ трогать `scripts-tmp/dashboard-server.ts` (W6.4 endpoint /api/paper2 и /papertrader2 UI).
- НЕ удалять и не модифицировать W6.1-коллекторы, дашборд-app, sa-* апы.
- НЕ запускать `pm2 start ecosystem.config.cjs` без `--only pt1-dip-discovery-test`.
- НЕ выставлять `PAPER_DIP_WHALE_ANALYSIS_ENABLED=1` для `pt1-dip-discovery-test` (создаст лавину тяжёлых SQL по всему `swaps` за 10 мин окна; whale включится в W6.5).
