# Spec W6.1 — DEX collectors port

**Назначение spec:** для агента-исполнителя (cheap LLM). Выполнить последовательно, не отступая.

**Контекст продукта:** `solana-alpha`. Репо: `c:/Users/cente/Ideas/solana-alpha`. Ветка `v2`. Текущий HEAD после W6.0: `2d2a25ab0c1c6c2d014a539ba59606fa7a3f3709`.

**Время выполнения:** ~90–120 минут.

## Цель

Поднять **6 коллекторов** как PM2-процессы, чтобы данные начали течь в таблицы из W6.0:

| Коллектор | Пишет в | Источник данных |
|---|---|---|
| `sa-raydium` | `raydium_pair_snapshots` | DexScreener + GeckoTerminal |
| `sa-meteora` | `meteora_pair_snapshots` | DexScreener + GeckoTerminal |
| `sa-orca` | `orca_pair_snapshots` | DexScreener + GeckoTerminal |
| `sa-moonshot` | `moonshot_pair_snapshots` | DexScreener + GeckoTerminal |
| `sa-jupiter` | `jupiter_route_snapshots` | Jupiter Quote API + локальные таблицы как candidate-source |
| `sa-direct-lp` | `direct_lp_events` | UNION `*_pair_snapshots` + `swaps` + `tokens` |

После выполнения PM2 на VPS = **4 (старых) + 6 (новых) = 10 процессов** online, в течение 3 минут данные пишутся, дашборд показывает health для каждого коллектора.

## Жёсткие ограничения

- НЕ модифицировать существующие коллекторы / процессы (`sa-stream`, `sa-parser`, `sa-atlas`, `dashboard-organizer-paper`).
- НЕ модифицировать `src/parser/**`, `src/stream/**`, `src/atlas/**`, `src/scripts/{stream,parser,atlas,migrate}.ts`.
- НЕ модифицировать `src/core/db/**` (включая `schema.ts`, миграции). W6.0 уже всё подготовил.
- НЕ модифицировать `docs/platform/**`.
- НЕ создавать таблицу `rpc_tasks` (отложено до W6.7+) — все упоминания должны быть выпилены или обойдены.
- НЕ пушить в `main`. Только в `v2` через squash из feature-ветки.
- НЕ использовать `git push --force` ни при каких обстоятельствах.
- НЕ устанавливать новые npm-зависимости (всё, что нужно — `pg`, `decimal.js`, `dotenv` — уже в `package.json`).

## Шаг 0 — Подготовка ветки и legacy-worktree

```bash
cd c:/Users/cente/Ideas/solana-alpha
git checkout v2
git pull origin v2
git checkout -b w6.1-dex-collectors

# legacy-worktree уже создан оркестратором; проверь что он на месте:
git worktree list | findstr _salvage_legacy   # должна быть строка с salvage/pre-v2
ls ../_salvage_legacy/scripts-tmp/raydium-collector.mjs  # должен существовать
```

Если worktree отсутствует — создай:

```bash
git worktree add ../_salvage_legacy salvage/pre-v2
```

**Что делать с salvage worktree после spec:** не удалять (пригодится для W6.3/W6.4).

## Шаг 1 — Скопировать 6 коллекторов и применить патчи

Все 6 файлов копируются ИЗ `c:/Users/cente/Ideas/_salvage_legacy/scripts-tmp/<file>.mjs` В `c:/Users/cente/Ideas/solana-alpha/scripts-tmp/<file>.mjs`. Затем — точечные правки.

### 1.1. `raydium-collector.mjs`

1. Скопировать `_salvage_legacy/scripts-tmp/raydium-collector.mjs` → `solana-alpha/scripts-tmp/raydium-collector.mjs`.
2. Применить патчи:
   - **Удалить строку:** `const RPC_FEATURES = ['holders', 'largest_accounts', 'authorities', 'tx_burst'];`
   - **Удалить целиком функцию `enqueueRpcTasks` (от `async function enqueueRpcTasks(shortlistedMints) {` до закрывающей `}`)**.
   - **Удалить из `collectOneTick`:**
     - строку `const rpcTasksEnqueued = await enqueueRpcTasks(shortlistedMints);`
     - поле `rpcTasksEnqueued,` из `log('info', 'tick completed', { ... })`.

### 1.2. `meteora-collector.mjs`

1. Скопировать `_salvage_legacy/scripts-tmp/meteora-collector.mjs` → `solana-alpha/scripts-tmp/meteora-collector.mjs`.
2. Патчи:
   - **Удалить строку:** `const RPC_TASK_PRIORITY = Number(process.env.METEORA_RPC_TASK_PRIORITY || 50);`
   - **Удалить строку:** `const RPC_FEATURES = ['holders', 'largest_accounts', 'authorities', 'tx_burst'];`
   - **Удалить функцию `enqueueRpcTasks` целиком.**
   - **В `collectOneTick`:**
     - удалить `const rpcTasksEnqueued = await enqueueRpcTasks(shortlistedMints);`
     - удалить поле `rpcTasksEnqueued,` из лога.
   - **В `main()` log:**
     - удалить поле `rpcTaskPriority: RPC_TASK_PRIORITY,`.

### 1.3. `orca-collector.mjs`

1. Скопировать `_salvage_legacy/scripts-tmp/orca-collector.mjs` → `solana-alpha/scripts-tmp/orca-collector.mjs`.
2. Патчи:
   - **Удалить строку:** `const RPC_TASK_PRIORITY = Number(process.env.ORCA_RPC_TASK_PRIORITY || 50);`
   - **Удалить строку:** `const RPC_FEATURES = ['holders', 'largest_accounts', 'authorities', 'tx_burst'];`
   - **Удалить функцию `enqueueRpcTasks` целиком.**
   - **Удалить функцию `ensureSchema` целиком** (таблица создана в W6.0 миграции; legacy DDL устарел и не совпадает с миграцией).
   - **В `main()` удалить `await ensureSchema();`** (строка перед `await runTickGuarded();`).
   - **В `collectOneTick`:**
     - удалить `const rpcTasksEnqueued = await enqueueRpcTasks(shortlistedMints);`
     - удалить поле `rpcTasksEnqueued,` из лога.
   - **В `main()` log:**
     - удалить поле `rpcTaskPriority: RPC_TASK_PRIORITY,`.

### 1.4. `moonshot-collector.mjs`

1. Скопировать `_salvage_legacy/scripts-tmp/moonshot-collector.mjs` → `solana-alpha/scripts-tmp/moonshot-collector.mjs`.
2. Патчи:
   - **Удалить:** `const RPC_TASK_PRIORITY = Number(process.env.MOONSHOT_RPC_TASK_PRIORITY || 50);`
   - **Удалить:** `const RPC_FEATURES = ['holders', 'largest_accounts', 'authorities', 'tx_burst'];`
   - **Удалить функцию `enqueueRpcTasks` целиком.**
   - **В `collectOneTick`:**
     - удалить `const rpcTasksEnqueued = await enqueueRpcTasks(shortlistedMints);`
     - удалить поле `rpcTasksEnqueued,` из лога.
   - **В `main()` log:**
     - удалить поле `rpcTaskPriority: RPC_TASK_PRIORITY,`.
   - **Унификация поля `source`:** в `normalizeDexScreenerPair` строку `source: 'dexscreener',` заменить на `source: 'moonshot',`. В `normalizeGeckoPool` строку `source: 'geckoterminal',` заменить на `source: 'moonshot',`. Обоснование: остальные коллекторы (raydium/meteora/orca) пишут `source = '<dex>'`, а downstream-запросы фильтруют по DEX, не по API-источнику.

### 1.5. `jupiter-route-watcher.mjs`

1. Скопировать `_salvage_legacy/scripts-tmp/jupiter-route-watcher.mjs` → `solana-alpha/scripts-tmp/jupiter-route-watcher.mjs`.
2. Патчи:
   - **Удалить функцию `ensureSchema` целиком** (таблица создана в W6.0 миграции).
   - **В `main()` удалить `await ensureSchema();`** (первая строка).
   - **НЕ трогать** функцию `enqueueRpcTasks` — там стоит guard `if (!ENQUEUE_RPC) return 0;` и через env она будет отключена. Соответственно `RPC_FEATURES` тоже оставить (он используется только в `enqueueRpcTasks`).
   - **НЕ трогать** SQL `buildCandidateSources()` — он сам делает `tableExists()`-чек на каждую таблицу и работает корректно с тем подмножеством таблиц, что есть.

### 1.6. `direct-lp-detector.mjs`

1. Скопировать `_salvage_legacy/scripts-tmp/direct-lp-detector.mjs` → `solana-alpha/scripts-tmp/direct-lp-detector.mjs`.
2. Патчи:
   - **Удалить функцию `ensureSchema` целиком** (таблица создана в W6.0 миграции).
   - **В `main()` удалить `await ensureSchema();`** (первая строка).
   - **`enqueueRpcTasks` оставить как есть** — там уже guard `if (!ENQUEUE_RPC || events.length === 0 || !(await tableExists('rpc_tasks')))` — он корректно вернёт 0 без таблицы.
   - **Адаптация `known_launches` под нашу схему:** в legacy SQL фигурируют фильтры на pumpportal-источник, которого у нас нет. Заменить:
     - В блоке `launchSources.push` (после `if (hasTokens)`):
       ```js
       launchSources.push(`
         SELECT mint AS base_mint, MIN(first_seen_at) AS launch_ts, 'tokens:pumpfun' AS launch_source
         FROM tokens
         WHERE mint IN (SELECT DISTINCT base_mint FROM swaps WHERE dex = 'pumpfun')
         GROUP BY mint
       `);
       ```
       (заменить старый блок с `metadata->>'source' = 'pumpportal'`).
     - В блоке `launchSources.push` (после `if (hasSwaps)`):
       ```js
       launchSources.push(`
         SELECT base_mint, MIN(block_time) AS launch_ts, 'swaps:pumpfun' AS launch_source
         FROM swaps
         WHERE dex = 'pumpfun'
         GROUP BY base_mint
       `);
       ```
       (заменить старый блок с `WHERE source = 'pumpportal'` — у нас в swaps `source='sa-parser'`, а признак pump.fun происхождения — это `dex='pumpfun'`).

## Шаг 2 — Расширить `ecosystem.config.cjs`

Добавить **6 новых apps** в массив `apps:`. Минимальная структура каждого app аналогична существующим, но `script: TSX` НЕ нужен — коллекторы это `.mjs`, запускаются напрямую через `node`.

Добавить в КОНЕЦ массива `apps` (после блока `sa-atlas`), сохранив существующие 4:

```js
{
  name: 'sa-raydium',
  cwd: root,
  script: 'scripts-tmp/raydium-collector.mjs',
  interpreter: 'node',
  exec_mode: 'fork',
  instances: 1,
  autorestart: true,
  max_restarts: 50,
  restart_delay: 5000,
  max_memory_restart: '300M',
  merge_logs: true,
  time: true,
  env: { NODE_ENV: 'production' },
},
{
  name: 'sa-meteora',
  cwd: root,
  script: 'scripts-tmp/meteora-collector.mjs',
  interpreter: 'node',
  exec_mode: 'fork',
  instances: 1,
  autorestart: true,
  max_restarts: 50,
  restart_delay: 5000,
  max_memory_restart: '300M',
  merge_logs: true,
  time: true,
  env: { NODE_ENV: 'production' },
},
{
  name: 'sa-orca',
  cwd: root,
  script: 'scripts-tmp/orca-collector.mjs',
  interpreter: 'node',
  exec_mode: 'fork',
  instances: 1,
  autorestart: true,
  max_restarts: 50,
  restart_delay: 5000,
  max_memory_restart: '300M',
  merge_logs: true,
  time: true,
  env: { NODE_ENV: 'production' },
},
{
  name: 'sa-moonshot',
  cwd: root,
  script: 'scripts-tmp/moonshot-collector.mjs',
  interpreter: 'node',
  exec_mode: 'fork',
  instances: 1,
  autorestart: true,
  max_restarts: 50,
  restart_delay: 5000,
  max_memory_restart: '300M',
  merge_logs: true,
  time: true,
  env: { NODE_ENV: 'production' },
},
{
  name: 'sa-jupiter',
  cwd: root,
  script: 'scripts-tmp/jupiter-route-watcher.mjs',
  interpreter: 'node',
  exec_mode: 'fork',
  instances: 1,
  autorestart: true,
  max_restarts: 50,
  restart_delay: 5000,
  max_memory_restart: '350M',
  merge_logs: true,
  time: true,
  env: {
    NODE_ENV: 'production',
    JUPITER_WATCHER_ENQUEUE_RPC: '0',
  },
},
{
  name: 'sa-direct-lp',
  cwd: root,
  script: 'scripts-tmp/direct-lp-detector.mjs',
  interpreter: 'node',
  exec_mode: 'fork',
  instances: 1,
  autorestart: true,
  max_restarts: 50,
  restart_delay: 5000,
  max_memory_restart: '300M',
  merge_logs: true,
  time: true,
  env: {
    NODE_ENV: 'production',
    DIRECT_LP_ENQUEUE_RPC: '0',
  },
},
```

**Важно:**
- 4 существующих apps (`dashboard-organizer-paper`, `sa-stream`, `sa-parser`, `sa-atlas`) остаются БЕЗ изменений.
- `interpreter: 'node'` нужен потому что `.mjs` нельзя запускать через `npm run` без скрипта в `package.json`.

## Шаг 3 — Обновить `.env.example`

Открыть `.env.example` и в КОНЕЦ файла добавить:

```bash
# ===== W6.1 — DEX collectors =====
# All collectors use sane defaults; override only if needed.

# Raydium / Meteora / Orca / Moonshot collectors (DexScreener + GeckoTerminal):
RAYDIUM_COLLECTOR_INTERVAL_MS=60000
METEORA_COLLECTOR_INTERVAL_MS=60000
ORCA_COLLECTOR_INTERVAL_MS=60000
MOONSHOT_COLLECTOR_INTERVAL_MS=60000

# Jupiter route watcher:
JUPITER_WATCHER_INTERVAL_MS=60000
JUPITER_WATCHER_LOOKBACK_HOURS=2
JUPITER_WATCHER_MAX_MINTS=20
JUPITER_WATCHER_QUOTE_IN_USD=25
JUPITER_WATCHER_SLIPPAGE_BPS=300
# CRITICAL: must be '0' until W6.7 (rpc_tasks table not created yet).
JUPITER_WATCHER_ENQUEUE_RPC=0

# Direct-LP detector:
DIRECT_LP_DETECTOR_INTERVAL_MS=60000
DIRECT_LP_FRESH_LOOKBACK_MINUTES=10
DIRECT_LP_HISTORY_LOOKBACK_HOURS=168
DIRECT_LP_MIN_LIQUIDITY_USD=0
# CRITICAL: must be '0' until W6.7 (rpc_tasks table not created yet).
DIRECT_LP_ENQUEUE_RPC=0
```

## Шаг 4 — Расширить `dashboard-server.ts` health endpoints

Открыть `scripts-tmp/dashboard-server.ts`. Найти существующий блок `app.get('/api/atlas/health', ...)` (≈ строка 425). После блока `app.get('/api/stream/health', ...)` (то есть после закрывающей `});` около строки 509, **перед** `app.listen(...)`) ВСТАВИТЬ:

```ts
const DEX_SOURCE_TABLES = {
  raydium: 'raydium_pair_snapshots',
  meteora: 'meteora_pair_snapshots',
  orca: 'orca_pair_snapshots',
  moonshot: 'moonshot_pair_snapshots',
} as const;

app.get<{ Params: { source: string } }>('/api/dex/:source/health', async (req, reply) => {
  reply.header('cache-control', 'no-store');
  const src = String(req.params.source || '').toLowerCase();
  const table = DEX_SOURCE_TABLES[src as keyof typeof DEX_SOURCE_TABLES];
  if (!table) {
    reply.code(404);
    return { ok: false, error: `unknown source: ${src}` };
  }
  try {
    const sql = pgPool();
    const [row] = await sql.unsafe(
      `SELECT
        (SELECT count(*)::bigint FROM ${table}) AS total,
        (SELECT count(*)::bigint FROM ${table} WHERE created_at > now() - interval '1 minute') AS m1,
        (SELECT count(*)::bigint FROM ${table} WHERE created_at > now() - interval '5 minutes') AS m5,
        (SELECT max(ts) FROM ${table}) AS last_bucket_ts,
        (SELECT max(created_at) FROM ${table}) AS last_inserted_at,
        (SELECT count(DISTINCT base_mint)::bigint FROM ${table} WHERE ts > now() - interval '1 hour') AS distinct_mints_h1`,
    );
    return {
      source: src,
      total: Number(row.total),
      m1: Number(row.m1),
      m5: Number(row.m5),
      last_bucket_ts: row.last_bucket_ts,
      last_inserted_at: row.last_inserted_at,
      distinct_mints_h1: Number(row.distinct_mints_h1),
    };
  } catch (e) {
    reply.code(503);
    return { ok: false, error: String(e) };
  }
});

app.get('/api/jupiter/health', async (_req, reply) => {
  reply.header('cache-control', 'no-store');
  try {
    const sql = pgPool();
    const [row] = await sql`
      SELECT
        (SELECT count(*)::bigint FROM jupiter_route_snapshots) AS total,
        (SELECT count(*)::bigint FROM jupiter_route_snapshots WHERE created_at > now() - interval '5 minutes') AS m5,
        (SELECT count(*)::bigint FROM jupiter_route_snapshots WHERE created_at > now() - interval '5 minutes' AND routeable = true) AS routeable_m5,
        (SELECT max(ts) FROM jupiter_route_snapshots) AS last_bucket_ts,
        (SELECT count(DISTINCT mint)::bigint FROM jupiter_route_snapshots WHERE ts > now() - interval '1 hour') AS distinct_mints_h1
    `;
    return {
      total: Number(row.total),
      m5: Number(row.m5),
      routeable_m5: Number(row.routeable_m5),
      last_bucket_ts: row.last_bucket_ts,
      distinct_mints_h1: Number(row.distinct_mints_h1),
    };
  } catch (e) {
    reply.code(503);
    return { ok: false, error: String(e) };
  }
});

app.get('/api/direct-lp/health', async (_req, reply) => {
  reply.header('cache-control', 'no-store');
  try {
    const sql = pgPool();
    const [row] = await sql`
      SELECT
        (SELECT count(*)::bigint FROM direct_lp_events) AS total,
        (SELECT count(*)::bigint FROM direct_lp_events WHERE created_at > now() - interval '1 hour') AS h1,
        (SELECT max(ts) FROM direct_lp_events) AS last_event_ts,
        (SELECT count(DISTINCT base_mint)::bigint FROM direct_lp_events WHERE ts > now() - interval '24 hours') AS distinct_mints_d1,
        (SELECT avg(confidence)::float FROM direct_lp_events WHERE ts > now() - interval '24 hours') AS avg_confidence_d1
    `;
    return {
      total: Number(row.total),
      h1: Number(row.h1),
      last_event_ts: row.last_event_ts,
      distinct_mints_d1: Number(row.distinct_mints_d1),
      avg_confidence_d1: row.avg_confidence_d1 != null ? Number(row.avg_confidence_d1) : null,
    };
  } catch (e) {
    reply.code(503);
    return { ok: false, error: String(e) };
  }
});
```

**Важно:**
- Шаблонный SQL (`sql\`...\``) и `sql.unsafe(...)` — оба используются в существующем коде. `sql.unsafe()` нужен для интерполяции имени таблицы (table name нельзя через шаблон).
- Не удалять существующие `/api/atlas/health`, `/api/parser/health`, `/api/stream/health`, `/api/state`, `/api/health`, `/api/visits` — они остаются нетронутыми.

## Шаг 5 — Локальная верификация

```bash
npm run typecheck
npm run lint
npm run build
npm run test    # passWithNoTests => OK
```

Все 4 — exit 0.

**Проверка коллекторов через `node --check`** (синтаксис .mjs):

```bash
node --check scripts-tmp/raydium-collector.mjs
node --check scripts-tmp/meteora-collector.mjs
node --check scripts-tmp/orca-collector.mjs
node --check scripts-tmp/moonshot-collector.mjs
node --check scripts-tmp/jupiter-route-watcher.mjs
node --check scripts-tmp/direct-lp-detector.mjs
```

Все 6 — exit 0 без вывода.

## Шаг 6 — Коммит

Список файлов, которые ДОЛЖНЫ быть закоммичены:

```
scripts-tmp/raydium-collector.mjs       (new)
scripts-tmp/meteora-collector.mjs       (new)
scripts-tmp/orca-collector.mjs          (new)
scripts-tmp/moonshot-collector.mjs      (new)
scripts-tmp/jupiter-route-watcher.mjs   (new)
scripts-tmp/direct-lp-detector.mjs      (new)
ecosystem.config.cjs                    (modified)
.env.example                            (modified)
scripts-tmp/dashboard-server.ts         (modified)
```

**НЕ коммитить:**
- Никакие правки в `src/**`.
- Никакие правки в `package.json`/`package-lock.json` (новых зависимостей нет).
- Никакие правки в `docs/platform/**`.
- Никакие правки в `deploy/`, `scripts-tmp/backup-*` и других файлах, которые могут быть «грязными» в рабочем дереве (они НЕ в scope spec).

```bash
git add scripts-tmp/raydium-collector.mjs scripts-tmp/meteora-collector.mjs scripts-tmp/orca-collector.mjs scripts-tmp/moonshot-collector.mjs scripts-tmp/jupiter-route-watcher.mjs scripts-tmp/direct-lp-detector.mjs ecosystem.config.cjs .env.example scripts-tmp/dashboard-server.ts

git commit -F - <<'EOF'
feat(collectors): W6.1 — port 6 DEX collectors as PM2 apps

Ported from salvage/pre-v2 with minimal adaptations:
- raydium / meteora / orca / moonshot collectors (DexScreener + GeckoTerminal,
  1-minute buckets, write to *_pair_snapshots tables created in W6.0)
- jupiter-route-watcher (Jupiter Quote API, routeable check before paper-trader entry)
- direct-lp-detector (UNION pair_snapshots vs swaps/tokens, infer direct-LP launches)

Adaptations from legacy:
- removed enqueueRpcTasks calls / RPC_FEATURES constants in 4 DEX collectors
  (rpc_tasks table is deferred to W6.7)
- jupiter & direct-lp keep enqueueRpcTasks but with ENQUEUE_RPC=0 forced via env
- removed legacy ensureSchema() blocks (W6.0 migrations are the source of truth)
- moonshot: unified source='moonshot' (was 'dexscreener'/'geckoterminal' in legacy)
- direct-lp: replaced source='pumpportal' filter with dex='pumpfun' to match v2 swaps

ecosystem.config.cjs: 6 new PM2 apps (sa-raydium / sa-meteora / sa-orca /
sa-moonshot / sa-jupiter / sa-direct-lp). Existing 4 apps untouched.

dashboard-server.ts: added /api/dex/:source/health (4 DEXes), /api/jupiter/health,
/api/direct-lp/health.

.env.example: added W6.1 collector envs with sane defaults.
EOF
```

## Шаг 7 — Push + squash to v2

```bash
git -c http.sslVerify=false push -u origin w6.1-dex-collectors

git checkout v2
git merge --squash w6.1-dex-collectors
git commit -F - <<'EOF'
feat(collectors): W6.1 — port 6 DEX collectors as PM2 apps

Ported from salvage/pre-v2 with adaptations to v2 schema:
- 4 DEX snapshot collectors (raydium / meteora / orca / moonshot)
- jupiter-route-watcher
- direct-lp-detector

ecosystem.config.cjs: +6 PM2 apps (existing 4 untouched).
dashboard-server.ts: +6 health endpoints.
.env.example: +collector envs (defaults).
EOF

git -c http.sslVerify=false push origin v2

git push origin --delete w6.1-dex-collectors 2>$null
git branch -D w6.1-dex-collectors
```

## Шаг 8 — Деплой на VPS

Используй SSH согласно `server-autodeploy.mdc`. SSH host: `187.124.38.242`, user: `root`, key: `c:/Users/cente/.ssh/botadmin_187_auto`. Рабочий каталог: `/opt/solana-alpha`. Service user: `salpha`.

**Совет для PowerShell:** не пиши длинные `psql/bash -lc` строки прямо в `ssh "..."` — будут проблемы с кавычками. Лучший способ: создай временный bash-скрипт локально, scp на VPS в `/tmp/`, выполни через ssh. После выполнения — удали временные `.sh` файлы из `scripts-tmp/`, чтобы не закоммитить случайно.

### 8.1. Бэкап БД

Скрипт `_w61_backup.sh` (создай локально, scp, выполни):

```bash
#!/usr/bin/env bash
set -euo pipefail
sudo -u salpha -H bash -lc 'cd /opt/solana-alpha && set -a && . ./.env && set +a && pg_dump --no-owner --no-acl "$DATABASE_URL" | gzip > /home/salpha/backups/before-w6.1-$(date -u +%Y%m%d-%H%M%S).sql.gz && ls -la /home/salpha/backups/before-w6.1-*.sql.gz'
```

Ожидание: файл ≥80 МБ.

### 8.2. Pull v2 + npm install (БЕЗ migrate — миграций в W6.1 нет)

```bash
#!/usr/bin/env bash
set -euo pipefail
sudo -u salpha -H bash -lc 'cd /opt/solana-alpha && git fetch origin && git reset --hard origin/v2 && npm install --omit=dev'
```

### 8.3. Обновить `.env` на VPS

CRITICAL: добавить **только два** ключа, чтобы `sa-jupiter` и `sa-direct-lp` не пытались писать в несуществующую `rpc_tasks`.

```bash
#!/usr/bin/env bash
set -euo pipefail
sudo -u salpha -H bash -lc '
  cd /opt/solana-alpha
  if ! grep -q "^JUPITER_WATCHER_ENQUEUE_RPC=" .env; then
    echo "" >> .env
    echo "# Added by W6.1 — required until rpc_tasks table exists (W6.7+)" >> .env
    echo "JUPITER_WATCHER_ENQUEUE_RPC=0" >> .env
  fi
  if ! grep -q "^DIRECT_LP_ENQUEUE_RPC=" .env; then
    echo "DIRECT_LP_ENQUEUE_RPC=0" >> .env
  fi
  echo "--- relevant env lines ---"
  grep -E "ENQUEUE_RPC|DATABASE_URL" .env | sed "s/DATABASE_URL=.*/DATABASE_URL=<masked>/"
'
```

### 8.4. Запуск 6 новых PM2 apps

```bash
#!/usr/bin/env bash
set -euo pipefail
sudo -u salpha -H bash -lc '
  cd /opt/solana-alpha
  pm2 start ecosystem.config.cjs --only sa-raydium,sa-meteora,sa-orca,sa-moonshot,sa-jupiter,sa-direct-lp
  sleep 3
  pm2 save
  pm2 list
'
```

**Если `pm2 save` выдаёт `[PM2][ERROR] Command not found`** — это известная безобидная ошибка из W5; процессы при этом стартуют корректно. Проигнорировать.

### 8.5. Прогрев — 3 минуты

```bash
sleep 180
```

### 8.6. Верификация

Скрипт `_w61_verify.sh`:

```bash
#!/usr/bin/env bash
set -euo pipefail
echo "=== HEAD ==="
sudo -u salpha -H bash -lc 'cd /opt/solana-alpha && git rev-parse HEAD'
echo "=== PM2 (expecting 10 online) ==="
sudo -u salpha -H bash -lc 'pm2 list'
echo "=== Table row counts (m5) ==="
sudo -u salpha -H bash -lc 'cd /opt/solana-alpha && set -a && . ./.env && set +a && psql "$DATABASE_URL" -c "SELECT '\''raydium'\'' AS dex, count(*) AS m5 FROM raydium_pair_snapshots WHERE created_at > now() - interval '\''5 minutes'\'' UNION ALL SELECT '\''meteora'\'', count(*) FROM meteora_pair_snapshots WHERE created_at > now() - interval '\''5 minutes'\'' UNION ALL SELECT '\''orca'\'', count(*) FROM orca_pair_snapshots WHERE created_at > now() - interval '\''5 minutes'\'' UNION ALL SELECT '\''moonshot'\'', count(*) FROM moonshot_pair_snapshots WHERE created_at > now() - interval '\''5 minutes'\'' UNION ALL SELECT '\''jupiter'\'', count(*) FROM jupiter_route_snapshots WHERE created_at > now() - interval '\''5 minutes'\'' UNION ALL SELECT '\''direct_lp'\'', count(*) FROM direct_lp_events WHERE created_at > now() - interval '\''5 minutes'\'';"'
echo "=== Health endpoints ==="
for src in raydium meteora orca moonshot; do
  echo "-- /api/dex/$src/health --"
  curl -sS "http://127.0.0.1:3008/api/dex/$src/health"
  echo
done
echo "-- /api/jupiter/health --"
curl -sS "http://127.0.0.1:3008/api/jupiter/health"
echo
echo "-- /api/direct-lp/health --"
curl -sS "http://127.0.0.1:3008/api/direct-lp/health"
echo
echo "=== Existing health (regression check) ==="
curl -sS http://127.0.0.1:3008/api/atlas/health; echo
curl -sS http://127.0.0.1:3008/api/parser/health; echo
curl -sS http://127.0.0.1:3008/api/stream/health; echo
echo "=== Public site ==="
curl -sS -o /dev/null -w "%{http_code}\n" https://etonne-moi.com/
echo "=== Last 30 lines of each new collector ==="
for app in sa-raydium sa-meteora sa-orca sa-moonshot sa-jupiter sa-direct-lp; do
  echo "--- $app ---"
  sudo -u salpha -H bash -lc "pm2 logs $app --nostream --lines 30 --raw" || true
done
```

**Важно:** `dashboard-organizer-paper` НЕ перезапускается автоматически после изменения `dashboard-server.ts`. Чтобы новые health endpoints заработали, надо его рестартнуть:

```bash
sudo -u salpha -H bash -lc 'pm2 restart dashboard-organizer-paper --update-env'
```

Это **должно быть выполнено ДО шага 8.6** (verify).

## Шаг 9 — Удалить временные файлы из репо

Перед финальным `git status` проверь, что в `scripts-tmp/` НЕТ файлов `_w61_*.sh` (они должны быть удалены после деплоя). Если есть — удали:

```bash
rm -f scripts-tmp/_w61_*.sh
```

Никаких новых коммитов делать НЕ нужно — эти файлы не попали в W6.1 коммит.

## Acceptance criteria (DoD)

После выполнения spec ВСЕ должны быть истинными:

1. **Git sync.** `git rev-parse HEAD` локально == `git ls-remote origin refs/heads/v2` == HEAD на VPS.
2. **PM2.** На VPS `pm2 list` показывает **10 процессов**, все `online`, uptime > 0:
   - `dashboard-organizer-paper`, `sa-stream`, `sa-parser`, `sa-atlas` (uptime может быть свежим только у `dashboard-organizer-paper`, остальные 3 — старые)
   - `sa-raydium`, `sa-meteora`, `sa-orca`, `sa-moonshot`, `sa-jupiter`, `sa-direct-lp` (свежие, uptime ≥3 минут)
3. **Данные текут.** Через ≥3 минуты прогрева:
   - `raydium_pair_snapshots`: `m5 ≥ 1`
   - `meteora_pair_snapshots`: `m5 ≥ 1`
   - `orca_pair_snapshots`: `m5 ≥ 1`
   - `moonshot_pair_snapshots`: `m5 ≥ 0` (мунщоты нечастые — допустимо 0, но `last_bucket_ts` НЕ должен быть NULL после нескольких минут)
   - `jupiter_route_snapshots`: `m5 ≥ 5` (хотя бы какие-то свопы кандидатов, routeable либо нет — не важно).
   - `direct_lp_events`: допускается `0` в первые часы (новых пар может не быть).
4. **Health endpoints.** Все новые endpoint отвечают **HTTP 200** с осмысленным JSON:
   - `GET /api/dex/raydium/health`, `/meteora`, `/orca`, `/moonshot` — каждый возвращает `{source, total, m1, m5, last_bucket_ts, last_inserted_at, distinct_mints_h1}`
   - `GET /api/jupiter/health` — `{total, m5, routeable_m5, last_bucket_ts, distinct_mints_h1}`
   - `GET /api/direct-lp/health` — `{total, h1, last_event_ts, distinct_mints_d1, avg_confidence_d1}`
5. **Регрессия.** Существующие endpoint без изменений:
   - `/api/atlas/health`: `lag_swaps < 1000`, `ew_m5 > 0`, `atlas_tags_m5 > 0`.
   - `/api/parser/health`: `m5 > 0`.
   - `/api/stream/health`: `m5 > 0`.
   - `https://etonne-moi.com/` → 200.
6. **Логи коллекторов.** В `pm2 logs <app> --lines 30` для каждого из 6 новых apps:
   - есть строка `"msg":"collector start"` или `"msg":"watcher start"` или `"msg":"detector start"`
   - есть **минимум одна** строка `"msg":"tick completed"` с осмысленными числами.
   - НЕТ строк `"level":"error"` со словами `relation "rpc_tasks" does not exist` или `column ... does not exist`.
7. **Бэкап.** `/home/salpha/backups/before-w6.1-*.sql.gz` существует, размер ≥ 80 МБ.
8. **Никаких новых таблиц.** `SELECT count(*) FROM pg_tables WHERE schemaname='public'` всё ещё возвращает **18** (W6.1 не вводит новых миграций).
9. **`db:generate` локально** → "No schema changes" (мы не трогали `schema.ts`).

## Финальный отчёт (что вывести в ответ оркестратору)

```
- HEAD локально: <sha>
- HEAD origin/v2: <sha>
- HEAD на VPS: <sha>
- PM2 list (10 процессов online, имена + uptime)
- Подсчёт m5 для каждой из 6 таблиц (одной таблицей)
- /api/dex/{raydium,meteora,orca,moonshot}/health, /api/jupiter/health, /api/direct-lp/health — JSON ответов (одной строкой каждый)
- Регрессия: /api/atlas/health, /api/parser/health, /api/stream/health (одной строкой)
- Sample log lines (по одной "tick completed" строке для каждого из 6 коллекторов)
- Бэкап: <путь и размер>
- Любые отклонения от spec с обоснованием
```

## Если что-то пойдёт не так

- **Коллектор стартует и сразу crash на `relation "rpc_tasks" does not exist`** — проверь, что:
  - В `.env` на VPS есть `JUPITER_WATCHER_ENQUEUE_RPC=0` и `DIRECT_LP_ENQUEUE_RPC=0` (для jupiter/direct-lp).
  - В 4 DEX-коллекторах ты **полностью удалил** `enqueueRpcTasks` функцию и её вызов (для raydium/meteora/orca/moonshot).
  - PM2 был перезапущен с `--update-env` после правки `.env`.
- **`/api/dex/<src>/health` 404** — проверь правильность копирования вставки в `dashboard-server.ts` И что `dashboard-organizer-paper` был рестартнут.
- **DexScreener 429** в логах — это нормально, у коллектора есть retry с backoff. Если 429 повторяется > 5 минут подряд — увеличь `*_COLLECTOR_INTERVAL_MS` до `120000` в `.env` на VPS и сделай `pm2 restart <app> --update-env`.
- **`db:migrate` сам собой запустился** — НЕТ. В этой spec migrate НЕ запускается, миграций нет. Если случайно запустил — проверь что `pg_tables` count всё ещё 18.
- **Не работает SSH** — действуй по `server-autodeploy.mdc`: сообщи пользователю, не делай manual fallback.
- **`pm2 save` ругается `Command not found`** — игнорировать (известно с W5).
- **Любое другое неожиданное поведение** — НЕ пытайся «починить» разово, сделай ESCALATE с полным выводом ошибки.
