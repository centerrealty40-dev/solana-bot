# Stripped for GitHub (push protection)

Original files env, env.hourly, salpha-pm2-jlist.json are NOT committed — they contain API tokens and DB passwords.

Keep them only on the VPS under /opt/solana-alpha/ or in your local _w0_salvage_payload / machine backup.

This branch still has: tlas.sql.gz, root/salpha crontabs, and deploy bootstrap artifacts on 2.
