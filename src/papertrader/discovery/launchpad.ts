import type { PaperTraderConfig } from '../config.js';

export async function fetchLaunchpadCandidates(_cfg: PaperTraderConfig): Promise<never[]> {
  // Implemented in W6.7 (Smart Lottery — uses entity_wallets.primary_tag).
  return [];
}
